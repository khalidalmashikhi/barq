// Placeholder home page. No business logic, no feature implementation.
// This file exists only to complete a valid, buildable Next.js App Router
// scaffold, per this project's rule that implementation follows
// documentation — every real feature below the platform level still
// requires its own approved specification before it is built here.

export default function HomePage() {
  return (
    <main>
      <h1>BARQ</h1>
      <p>Smart Tourism Operations Platform — implementation starting.</p>
    </main>
  );
}
