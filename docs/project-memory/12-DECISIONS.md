# 12 — Decisions Log

A running log of real, **granular/tactical** decisions made during implementation work — the "we chose X over Y for this specific reason" calls made *while building something*, in the spirit of `docs/08-governance/DEVELOPMENT_LOG.md`. Append to this file as new decisions are made — do not rewrite history in it.

**Scope split from `17-CHANGELOG-DECISIONS.md`**: this file is for implementation-detail decisions (a schema shape choice, an enum reuse, a file-organization call) made in service of a larger goal. `17-CHANGELOG-DECISIONS.md` is for the higher-level architectural/product decisions those tactical choices serve (a new ADR, a product-direction commitment). If a decision here was made *in service of* a `17-CHANGELOG-DECISIONS.md` entry, link to it rather than re-explaining the higher-level context.

## Entry format

```
### YYYY-MM-DD — Short decision title
**Context:** why this came up
**Decision:** what was decided
**Alternative(s) considered:** what else was on the table, and why it lost
```

---

### (project-memory phase) — Reuse `BookingActorType` for the new `AuditLog` model rather than a new enum
**Context:** Phase 5.2 added a general `AuditLog` model for admin/provider mutations. See BR-019 in `16-BUSINESS-RULES.md` and the `AuditLog` entity in `15-DATA-DICTIONARY.md` for the resulting rule/entity this decision produced.
**Decision:** Reused the existing `BookingActorType` enum (`CUSTOMER`/`PROVIDER`/`SYSTEM`/`ADMIN`) rather than defining a duplicate, identically-shaped enum under a more generic name.
**Alternative considered:** A new `AuditActorType` enum — rejected as unnecessary duplication (DRY) since the existing enum's values are already generic actor-kind values despite its booking-era name.

### (project-memory phase) — `docs/project-memory/` created as a 13th, deliberately un-numbered `docs/` subdirectory
**Context:** No AI-agent-facing memory system existed; the 12 numbered `docs/` subdirectories are ADR-gated and versioned, which is the wrong update cadence for living working memory.
**Decision:** New `docs/project-memory/` sits alongside the numbered directories, explicitly exempt from the ADR/RFC gate — update it freely as things are learned.
**Alternative considered:** Making it `docs/12-project-memory/` to match the numbering convention — rejected because that would visually imply it's part of the same versioned/frozen documentation register, which it deliberately is not.

### (project-memory phase) — Category model design deferred, not started
**Context:** The product direction calls for a full category/visibility system (planned engine #2), but no schema design has been done yet.
**Decision:** Documented the target state (`07-CATEGORIES.md`) and open design questions (`13-OPEN-QUESTIONS.md`) without proposing a schema — this phase is documentation-only by explicit instruction.
**Alternative considered:** Sketching a draft `Category` model now — rejected; schema design belongs in a dedicated, separately-approved implementation phase, not folded into a memory-scaffolding phase.

### 2026-08-12 — Core Service Enrichment (Gate 3): `Service.regionCode` + `Price.pricingUnit` wired as domain read/write only
**Context:** Gate 2 (local commit `1fde9db`, migration `20260812120000_service_region_pricing_unit`, still UNAPPLIED) added two nullable columns: `Service.regionCode` (broad Oman **governorate** where a service operates — a discovery/filter facet) and `Price.pricingUnit` (the display basis of `amount`). Gate 3 wires application read/write for them **without any UI, Explore filter, or presentation change**.
**Decision:**
- Two isomorphic code registries were added as the app-level allow-lists: `src/lib/regions` (the **11** governorate codes, mirroring the Gate-2 DB CHECK exactly) and `src/lib/pricing-units` (**5** codes: `PER_PERSON, PER_BOOKING, PER_DAY, PER_HOUR, PER_TRIP`). Both follow the `service-types/registry.ts` convention (ADR-0015: String codes, not Prisma enums) and each exposes a `parseRegionCode`/`parsePricingUnit` three-state helper (`null` = unset, code = valid, `undefined` = invalid).
- `regionCode` is a **governorate-level discovery facet only** — NOT a location/geography system. There is deliberately **no** wilayat/city/map/lat-lng/meeting-point/route/PostGIS, and **no** Vehicle/Asset entity.
- `pricingUnit` is **display/commercial metadata ONLY**. It rides the *same* ACTIVE `Price` row as `amount`/`currency` in every reader and **does not affect totals or booking behaviour** — nothing multiplies by it, `Booking` was not touched, no `priceSnapshotUnit` was added, and `createBooking`/payment/refunds are unchanged. On update it is set via a metadata-only `updateMany` on the ACTIVE price(s) — never a new Price row (append-only price versioning preserved, no duplicate ACTIVE price).
- The DB intentionally has **no** CHECK on `pricingUnit` (commercially extensible), so `src/lib/pricing-units` is its *only* allow-list; `regionCode` keeps its DB CHECK as defence-in-depth.
- Invalid `regionCode`/`pricingUnit` reuse the existing `INVALID_INPUT` error (Gate-4 UI will be dropdowns, so an invalid value is a malformed/spoofed request) rather than adding new error codes + 8-locale i18n keys.
- Update semantics: for these freely-nullable fields, an **absent** form field leaves the value unchanged while a **present-but-empty** field clears it to NULL — deliberately different from `categoryId`'s "empty = leave unchanged" (a category is required at publish and must never be silently nulled). All readers/actions are NULL-tolerant for legacy rows; no backfill.
**Alternative(s) considered:** Dedicated `INVALID_REGION`/`INVALID_PRICING_UNIT` error codes (rejected: needs new codes + translation keys across 8 locales for a spoof-only path); mutating/versioning a new Price row to change `pricingUnit` (rejected: it is metadata, not a price change); giving `pricingUnit` a DB CHECK (rejected in Gate 2 for commercial extensibility).

### 2026-08-12 — Core Service Enrichment (Gate 4): region/pricing-unit UX, public presentation, and the governorate Explore filter
**Context:** Gates 2/3 made `Service.regionCode` and `Price.pricingUnit` exist and be read/written by the domain (local commits `1fde9db`, `9d55c6c`). Gate 4 makes them useful to providers, admins, and customers without disturbing any invariant.
**Decision:**
- **Labels centralized in the `common` i18n namespace** (`governorate.*`, `pricingUnit.*`, `priceWithUnit`), added to **all 8 locales**, with code→key bridges `src/lib/regions/labels.ts` + `src/lib/pricing-units/labels.ts` (`as const satisfies Record<...>`, mirroring `SERVICE_TYPE_LABEL_KEYS`). The registry stays authoritative for validity; i18n for labels. This is the ONE place a code becomes a label — no per-surface switch (item 12). `regionLabelKey`/`pricingUnitLabelKey` fail safe to `null` for unknown/absent, so presentation never leaks a raw code (item 21) — the only guard for `pricingUnit`, which has no DB CHECK.
- **Shared field components** `RegionField` + `PricingUnitField` (async server components consuming the registries + `common` i18n) — native `<select>`s that submit the stable CODE and display the localized name, reused across provider create/edit + admin create/edit. No governorate/unit list is duplicated in any form.
- **Admin surface honesty (item 6):** admin *create* takes only `regionCode` (that path provisions no Price, so there is no unit target); the admin *edit* form shows `PricingUnitField` only when an ACTIVE price exists. Provider and admin share the same registries/validation.
- **Public presentation via the shared `ServiceDetailView`** (one component → public detail + provider preview + admin preview, no forking): a governorate tile (omitted when null) and price-with-unit via the `priceWithUnit` template. Public cards reuse the existing `ExperienceCard` contract — the server page resolves labels and passes ready display strings (governorate via the existing `location` slot, price+unit via `price`), so the client card gains no i18n/registry coupling.
- **Currency formatting left as-is:** price stays the pre-formatted `"25 OMR"` string produced in the query layer; Gate 4 only appends the localized unit. Migrating to `formatCurrency` is a separately-deferred concern (documented in `format-number.ts`) and out of scope here.
- **Explore governorate filter** on `/services`: `?region=<CODE>`, URL-addressable, validated against the registry (unknown → silently ignored, like an unknown `providerId`/`category`), added to `getServices` as a `regionCode` where-clause that **composes via AND** with the relational category filter and every other filter. No customer-facing `pricingUnit` filter (item 15) — region is the discovery facet; the unit is price-display metadata.
**Alternative(s) considered:** Duplicating governorate/unit labels into the `provider`/`admin`/`services` namespaces (rejected: DRY — one `common` home consumed everywhere); passing raw codes to the client card and resolving there (rejected: unnecessary client i18n coupling; the server resolves and passes strings); a new i18n namespace for governorates (rejected: `common` is already loaded everywhere, avoids wiring a new namespace into `namespaces.ts`/`types.d.ts`).

### 2026-08-15 — Provider Mutation API v1 (Gate PC): pre-auth wrapper, and standalone price mutation DEFERRED (no domain to adapt)
**Context:** Gate PC exposes the existing provider write operations (profile/service/availability/booking-action Server Actions) as thin `/api/v1/me/provider/**` JSON adapters for the native iOS client (ADR-0018). LOCAL commit `6520000` on `staging` — NOT pushed/deployed, VERIFIED BY TEST ONLY. Two decisions were forced during implementation.
**Decision:**
- **A pre-authenticating mutation wrapper (`withApiV1ProviderMutation`) was introduced** rather than reusing the read gate (`withApiV1Provider`). Every provider Server Action handles an unauthenticated session by calling `redirect("/")` (a browser-form convenience) — inside a route handler that throws `NEXT_REDIRECT` and emits a **307**, not a clean JSON **401**. The wrapper runs `requireProvider()` *before* invoking the action, so on no/expired session it returns 401 and the action is never reached (its redirect path is unreachable). It uses the LOOSEST gate (`requireProvider`, not `requireApprovedProvider`) so an APPLIED provider is admitted and then correctly told `PROVIDER_NOT_APPROVED` *by the action's own* stricter gate as a result code — never blocked at the wrapper (matching the web's own gating split).
- **Standalone provider price mutation is DEFERRED — because no provider price-edit domain exists to adapt.** The initial ACTIVE `Price` is created *bundled inside* `createService` (the provider's only price write), and `update-service.ts` explicitly records price-edit as "a distinct future decision, not made here" (Price is append-only/versioned). Exposing a price-mutation endpoint would require **inventing new domain logic**, which violates the thin-adapter mandate — so Gate PC exposes price only at service creation and adds no price-mutation route. Also deferred (same "no safe existing domain to thinly adapt within this gate" reasoning): verification-document upload (multipart + storage orchestration already owned by `/api/provider/documents`), provider media (Supabase Storage still inert), and provider application/resubmission (distinct onboarding concern; `applyAsProvider` targets users with *no* provider, incompatible with the provider-mutation gate). **Provider payments/payouts: NOT STARTED.**
- **The shared provider auth-error → envelope mapping was extracted into one pure, server-only-free module (`provider-auth-error.ts`)** now used by BOTH gates — statically testable against the real error classes, which also root-fixed the `ForbiddenError` class-field `instanceof`-across-mock-boundary test failure. Ownership stays at the domain boundary (no client `providerId`; missing/not-owned → uniform 404); success re-reads via the existing ownership-scoped GET reader and returns the existing DTO (no raw Prisma objects). Mutations are test-verified, **not** live-QA'd in this gate.
**Alternative(s) considered:** Reusing the read gate and catching `NEXT_REDIRECT` to synthesize a 401 (rejected: brittle — depends on Next's internal redirect error shape; pre-auth is explicit and robust). Adding a provider price-edit endpoint that inserts a new versioned Price row (rejected: that price-versioning semantics is an un-made domain decision — building it here would be new business logic, not a thin adapter). Building a JSON/multipart document-upload twin of `/api/provider/documents` (rejected: duplicates upload orchestration; infra live-status also unconfirmed).

### 2026-08-15 — P0-1 security gate: authoritative server-side Oman phone canonicalization before OTP
**Context:** The Full Platform Audit flagged P0-1 — OTP send accepted the raw client phone string with no server-side normalization or country restriction, while Twilio is live on staging. Consequences: SMS-pumping/toll-fraud to arbitrary international numbers, per-number cooldown/daily-cap bypass via format variation (`+96890…`/`96890…`/`0096890…`/`98…` are distinct identifier strings), and identity fragmentation (`User.phoneNumber` unique on the exact string). Scope: close ONLY P0-1 — not an auth redesign, no durable/IP limiter (separate P1).
**Decision:**
- **One authoritative normalizer** `src/lib/otp/normalize-oman-phone.ts` (pure, no I/O, no logging) canonicalizes every accepted Oman representation to `+968XXXXXXXX` (8 national digits) and REJECTS wrong lengths, non-digit input, and any explicit non-`+968` country code — never silently coercing a foreign number to Oman. Accepted: bare 8 digits, `+968…`, `968…` (11 digits), `0096898…`, plus spaces/dashes/parentheses/dots.
- **Wired into the Better Auth `hooks.before` for BOTH `/phone-number/send-otp` and `/phone-number/verify`** as the first action; it rewrites `ctx.body.phoneNumber` to the canonical value via the returned `{ context: { body } }`. Confirmed against the installed Better Auth (`dist/api/dispatch.mjs` deep-merges a before-hook's `context.body` into the endpoint context before the handler + after-hook run), so the resend cooldown, daily cap, `Verification.identifier`, Twilio delivery, verify lookup, and resulting `User` identity ALL key on the same canonical number. Invalid/non-Oman input is rejected **before** the cooldown/cap lookups, before any code is generated, and before Twilio is ever called (stable code `INVALID_PHONE_NUMBER`; the client maps it to a localized `auth.invalidPhoneNumber` message added to all 8 locales).
- **Existing-user compatibility:** repository/seed/test evidence (bootstrap-staging-admin `+968…`, all fixtures `+968…`, the onboarded `+96898115159`) shows the established identity convention is already canonical `+968` E.164, and the normalizer is a **no-op** for canonical input — so returning users are unaffected and NO identity migration is created here. Before the (separate, approved) deploy, a read-only DB audit of distinct `User.phoneNumber` formats should confirm zero non-canonical rows. Better Auth + Twilio architecture unchanged; no schema/migration. **VERIFIED BY TEST; NOT YET VERIFIED LIVE** (local commit only, not pushed/deployed).
**Alternative(s) considered:** Client-side normalization as the boundary (rejected — never authoritative; kept only as optional UX). Duplicating normalization in send/verify/cooldown/cap/lookup (rejected — the whole bypass came from divergent representations; one authority is the fix). Restricting the national number's leading digit to mobile prefixes 7/9 (deferred — the `+968` country restriction is the abuse control; over-restricting risks rejecting valid numbers). A one-time identity-canonicalization migration (not done — evidence says existing users are already canonical; a breaking migration without DB proof is out of this gate per its own rules).

### 2026-08-15 — P1 security gate: durable distributed OTP rate limiting (dedicated Postgres `AuthRateLimit` table)
**Context:** The audit's P1 — the in-memory limiter (`rate-limit/rate-limiter.ts`, booking/review only) is process-local, so on Vercel each warm instance keeps its own counter and cold starts reset it (effective ceiling `limit × instances`, not a global cap). The per-phone OTP controls (30s cooldown + daily cap) were already DB-backed/durable, but there was **no per-IP control** on the auth surface. Approved store: **Option 1 — a dedicated PostgreSQL table** (reuse the DB BARQ already depends on; explicitly NOT Redis/Upstash/KV and NOT the Better Auth `verifications` table).
**Decision:**
- New model **`AuthRateLimit`** (`auth_rate_limits`: `key` PK, `count`, `windowStart`, `expiresAt`, index on `expiresAt`) + an additive-only migration (`20260815120000_auth_rate_limit`) — **created, NOT applied to staging in this gate.** Stores ONLY a fixed-window counter — never a raw IP/phone/OTP/session/token/PII. Better Auth tables untouched.
- **Atomic** counter via a single `INSERT … ON CONFLICT DO UPDATE … RETURNING` (`durable-rate-limiter.ts`) evaluated against the DB clock — concurrent requests serialize on the row's write lock (same mechanism as the booking oversell guard), so there is no count-then-increment race and no separate `SELECT`. **Fail-closed** on any store error (deny + log without PII); since the store IS the primary Postgres the OTP flow already needs, a "store outage" is not an attacker-controllable bypass.
- **Trusted IP** (`rate-limit/client-ip.ts`): resolves the client IP from **Vercel's platform-set `x-real-ip` ONLY** (a single value the client cannot forge). **`x-forwarded-for` is deliberately NOT trusted** — its leftmost token is client-spoofable (Better Auth's own `getIPFromHeader` documents and refuses this), so trusting it would let an attacker rotate the value to evade the IP limiter. A missing/multi-valued `x-real-ip` → one shared `unknown` bucket (LIMITS, never bypasses). *(Pre-push review correction: an earlier draft fell back to the leftmost `x-forwarded-for`; that spoofable fallback was removed before the P1 commit was pushed.)*
- **No PII in the store:** BOTH identities are **HMAC-SHA256'd with the existing `BETTER_AUTH_SECRET`** (one shared primitive `hmacRateLimitKey`, no new secret) before becoming a key — the IP hash and the **HMAC of the canonical +968 phone**. `auth_rate_limits.key` therefore holds only opaque digests: **no raw IP and no raw phone**, never logged raw.
- **Scopes** (checked in the Better Auth `hooks.before`, after P0-1 canonicalization, before any code generation / Twilio): SEND → durable per-IP cap (default 15/h — the new cross-phone control) + durable per-phone cap keyed on the HMAC of the canonical +968 phone (default 6/h), **on top of** the preserved 30s cooldown + daily cap; VERIFY → durable per-IP cap (default 30/h). **No per-phone verify cap** — that would enable a victim-phone lockout DoS; per-code brute force stays capped by Better Auth's `allowedAttempts` (3). Keys are namespaced (`otp:send:ip:` / `otp:send:phone:` / `otp:verify:ip:`), no cross-limiter collision.
- **TTL:** lazy window reset inside the atomic UPSERT + opportunistic best-effort sweep of long-expired rows (5% of sends, fire-and-forget) — **no new cron**. **429 contract:** reuses `TOO_MANY_REQUESTS` with a generic message (no counter/key/IP/phone/store detail) — no web/iOS client change.
- Tunable via optional `AUTH_OTP_*` env vars (fail-fast on malformed). Better Auth + Twilio architecture unchanged; no external service, no new secret. **VERIFIED BY TEST; migration NOT applied to staging; live QA pending.**
**Alternative(s) considered:** Upstash/Vercel KV/Redis (rejected — new external service + secrets + cost; Postgres suffices, per the approved scope). Better Auth's built-in DB rate limiter (rejected — also needs its own table/migration, is IP-only, and is awkward to combine with the canonical-phone dimension). Reusing the `verifications` table for IP counters (rejected — overloads a Better-Auth-owned table whose shape is unverified). A per-phone verify limiter (rejected — victim-lockout DoS).

### 2026-08-15 — Global-ready phone-entry UI (country picker), authentication still OMAN-ONLY
**Context:** The web login used a single free-text phone field. We want a polished, international-app-style entry — a country selector integrated into the phone field with a searchable picker — WITHOUT enabling international authentication. BARQ phone auth remains Oman-only (P0-1/P1 unchanged).
**Decision:**
- **UI is globally structured; auth is Oman-only.** New `src/lib/countries/registry.ts` holds a curated `Country[]` ({iso, callingCode, nameEn, nameAr, authSupported}); **exactly one** country has `authSupported: true` (Oman). Every other is shown with a "coming soon" chip and is **not selectable for auth**. Flags are derived from ISO (`flag.ts`, regional-indicator emoji), never stored. No IP/geo detection.
- **Search (`search.ts`, local/API-free)** matches English name, Arabic name (diacritic/alef-tolerant so `عمان` matches `عُمان`), ISO code, and calling code with/without `+`.
- **`phone-entry.ts` (`resolveAuthPhone`) is the one gate** turning (country + national input) into the identity sent to Better Auth. For Oman it **reuses the P0-1 canonicalizer `normalizeOmanPhone` — never re-implemented** — so the value sent is byte-for-byte the same canonical `+968XXXXXXXX`; an unsupported country returns `COUNTRY_UNSUPPORTED` and **never reaches send-otp** (the server's P0-1 + P1 remain authoritative regardless). Pure, unit-tested (this repo has no jsdom → security logic lives in `.ts`, not `.tsx`).
- **Components:** `CountryPicker` (searchable overlay — mobile bottom-sheet / desktop dialog, ESC/backdrop close, focus-on-open, `type="button"` rows + a guarded search Enter so changing country can never submit the form) and `PhoneNumberInput` (`[flag · callingCode · ▾]` + national field, calling code/number forced LTR). `login-form.tsx` now resolves via `resolveAuthPhone`, gates submit on `canRequestOtp`, and reuses one resolved `submittedPhone` for send/resend/verify (the `sendOtp`/`verify` call shapes are unchanged). i18n keys added to all 8 locales.
- **Enabling another country later is a PAIRED change** and must never be one-sided: flip `authSupported` in the registry AND wire that country's server-side canonicalizer + OTP policy together.
**Preserved (verified by regression):** P0-1 canonicalization, P1 durable IP/phone rate limiting, HMAC privacy, `x-real-ip` trust, cooldown/daily cap, Better Auth verify-attempt caps — all unchanged; no backend/schema/env/Twilio change. **VERIFIED BY TEST; not pushed/deployed.**
**Alternative(s) considered:** A large country-data dependency such as `i18n-iso-countries`/`libphonenumber` (rejected — a lightweight, hand-verified internal dataset is more appropriate per the repo's dependency discipline; flags derive from ISO). Client-side canonicalization as authority (rejected — the server P0-1 stays authoritative; the client reuses the same pure normalizer only for UX parity). Full ~250-country list (deferred — a curated set is accurate for the Arabic names and trivially extendable).

### 2026-08-16 — Provider Verification 2.0 · Gate 0: fix upload failure + auto-upload + real progress (no schema)
**Context:** A real provider saw the generic Arabic upload error. Root-cause trace (audit + code): the upload is a progressive `<form>` POST to the route handler `/api/provider/documents` → `uploadProviderDocument()` (auth/ownership → type → size/MIME/magic-byte **before** storage → private-bucket write → DB+audit txn). The exact string is `documentErrorUnknown` (code `UNKNOWN_ERROR`), emitted from **only two** catch blocks: the Supabase upload throwing, or a non-P2002 DB write throwing. Staging env has all three storage vars set (`isDocumentStorageConfigured()` = true), so the failure reached the real storage write or the DB write and threw — the generic message **hid which**. The physical private bucket's existence was **not** verifiable read-only without pulling the service-role key (an unsafe secret pull) or Supabase-dashboard access; not reproduced with a synthetic upload (would be an unapproved live mutation). Gate 0 = smallest safe code fix + the auto-upload UX; provisioning/verifying the bucket stays an operator step.
**Decision:**
- **Error contract split (root-cause fix):** a failed **private-bucket write** now returns the distinct, actionable `UPLOAD_FAILED` (HTTP 502, message *"The document could not be uploaded right now. Please try again."* / *"تعذر رفع الملف حاليًا. حاول مرة أخرى."*) in both `uploadProviderDocument` and `replaceProviderDocument`; a **DB-write** failure keeps `UNKNOWN_ERROR` (true internal fallback). The existing `uploadProviderDocument.storage_failed` vs `.db_failed` logs already separate the two for the operator. Same convention as the media surface's `UPLOAD_FAILED`.
- **Auto-upload + REAL progress:** new client control `verification-document-control.tsx` — choosing a file starts the upload automatically (no separate Upload button), the native "Choose File" chrome is visually hidden behind a BARQ button, and progress is genuine (`xhr.upload.onprogress`: determinate percent + bytes when `lengthComputable`, honest indeterminate bar otherwise — **never a faked timer**). Cancel = `xhr.abort()`; retry re-sends the kept file; a duplicate selection while uploading is a no-op. All decisions live in the pure, unit-tested `upload-ui.ts` (repo has no jsdom → logic in `.ts`, thin `.tsx` shell — same pattern as the phone-entry gate).
- **Transport:** the three routes (`upload`/`replace`/`delete`) are now **content-negotiated** — `Accept: application/json` (the XHR client) gets a JSON `{ ok, error?, documentId? }` with the code's HTTP status (via the pre-existing, previously-unused `providerDocumentErrorHttpStatus`); every other caller keeps the original progressive-`<form>` 303 redirect. Same server validation/storage/DB/audit path; additive, non-breaking.
- **Civil-ID-only wording (Individual):** the `IDENTITY_PROOF` default description changed from *"national ID or passport"* to *"a valid Civil / National ID card"* (EN) / *"البطاقة الشخصية / المدنية سارية الصلاحية"* (AR). On staging the seed is still pending, so the code default (`default-requirements.ts`) is what renders; the bootstrap seeds from the same source, so they can't drift. **Passport stays a non-uploadable type — no server accepted-TYPE rule was weakened.**
- **NOT in Gate 0 (later approved gates):** OCR/expiry automation, semantic document classification, the no-code policy engine, any schema/migration/lifecycle change. No schema, migration, DB mutation, auth/Twilio/env change, OTP, push, or deploy.
**Alternative(s) considered:** Direct-to-storage signed upload (deferred — real progress via XHR to the existing route is smaller and covers ≤4 MB ID cards/CRs; direct-to-storage is the >4 MB growth path). Faking a progress percentage (rejected outright). Adding a `DRAFT` lifecycle here (deferred to Gate 1 — schema/lifecycle change). Creating/adjusting the Supabase bucket to "fix" the failure (STOP-GATE — infrastructure action, not silently performed).

### 2026-08-16 — Provider Verification 2.0 · Gate 1A: explicit DRAFT → Submit → UNDER_REVIEW lifecycle
**Context:** Live QA (after the storage bucket was fixed) showed the workflow was incomplete: uploading a document immediately read as "Pending review" with no explicit "Submit for verification" step, so upload was being conflated with submission. Approved Gate 1A only (no OCR/expiry/official-record/admin-workspace/policy-engine).
**Decision:**
- **Lifecycle via `ProviderStatus` (not a separate case model — deferred):** added `DRAFT` (new self-service initial state) and gave `UNDER_REVIEW` its first writer. `applyAsProvider` now creates `DRAFT`; the provider uploads/replaces/deletes freely; an EXPLICIT `submitProviderVerification()` transitions `DRAFT → UNDER_REVIEW`. `resubmitProviderApplication` now returns `REJECTED → DRAFT` (edit, then re-submit). **`APPLIED` kept as a legacy "submitted" alias** — existing/admin-created providers stay reviewable (pending queue + approve/reject still accept `{APPLIED, UNDER_REVIEW}`); no new self-service write targets APPLIED, and no existing row was migrated. The schema `@default` stays `APPLIED` so the additive migration never USES the new enum value in its own transaction (a Postgres restriction); both create sites set status explicitly.
- **Submit is server-authoritative, session-owned, transactional, idempotent:** ownership from `requireProvider()` only; `assertReadyToSubmit` (new, presence-only) must return no blockers; the transition is an OPTIMISTIC `updateMany WHERE status='DRAFT'` + audit `provider.verification_submitted` in one transaction; a duplicate/raced submit matches 0 rows and returns an idempotent success (no second transition/audit). Already-`UNDER_REVIEW` → no-op success; any other state → `INVALID_STATE`.
- **Gate 1A readiness is PRESENCE-ONLY (`resolveSubmitBlockers`, distinct from the approval gate `resolveRequiredDocumentBlockers`):** a required doc MISSING or admin-REJECTED blocks Submit; a PENDING/APPROVED doc satisfies it. This makes NO claim about semantic type or expiry — automated document-intelligence is Gate 1C. The admin APPROVAL gate (`assertProviderApprovable`, still requiring admin-APPROVED docs) is unchanged and un-weakened; DRAFT is never admin-approvable.
- **UI:** prominent "Submit for verification / إرسال للتحقق" after the checklist, disabled until `canSubmit`; document mutation (upload/replace/delete) is offered ONLY while `editable` (DRAFT), locked once submitted (View stays); an "under review" note when UNDER_REVIEW; a DRAFT branch on `/provider-application`. 8-locale strings added. `DRAFT` added to all four exhaustive `Record<ProviderStatus,…>` maps + a `statusDraft` admin badge label.
- **Schema:** ONE additive migration `20260816120000_provider_draft_status_and_submitted_at` — `ALTER TYPE ADD VALUE 'DRAFT'` + nullable `Provider.submittedAt`. Non-destructive; no backfill.
**Staging application (NOT done by me — no safe staging DB access):** the migration reaches staging only via `vercel-build` (`prisma migrate deploy`) on a deploy — deferred, needs separate deploy approval. The approved passport-description data UPDATE (`provider_verification_requirements` IDENTITY_PROOF) also needs staging DB access I don't hold; prepared, run by the owner in a staging-pointed environment. Local: migration authored offline via `prisma migrate diff` (no DB touched), client regenerated, full suite green. **Local commit only; not pushed.**
**Alternative(s) considered:** A dedicated `ProviderVerificationCase` + `policyVersion` (deferred — cleaner for re-verification/versioning but heavier; layered on later, hence new tables will reference providerId + a nullable caseId when they arrive). Changing the schema `@default` to DRAFT (rejected for Gate 1A — would make the migration use the new enum value in-transaction; both create sites are explicit so the default never fires). Adding `CHANGES_REQUESTED` now (deferred — no admin "request changes" action exists until the Gate 1B admin workspace, so it would be an unused value).

### 2026-08-16 — Provider Verification 2.0 · Gate 1B: admin review workspace + Request Changes (CHANGES_REQUESTED)
**Context:** Gate 1A (DRAFT→submit→UNDER_REVIEW) is deployed (staging `7e0fd51`). Live QA needed a proper admin review experience and an admin outcome distinct from permanent rejection. Audit finding: the admin `/admin/providers/[id]` page ALREADY had a Gate-3 review section (per-doc View/Approve/Reject-with-reason, versionToken RC3, `assertProviderApprovable` blockers + disabled-Approve) — so Gate 1B ENHANCED it rather than rebuilding, and added the genuinely-new Request Changes flow.
**Decision:**
- **New status `ProviderStatus.CHANGES_REQUESTED`** (approved schema STOP; one additive migration `20260816160000`, `ALTER TYPE ADD VALUE` only, no new column, no backfill) — the clean separation the owner required: `REJECTED` = denied; `CHANGES_REQUESTED` = returned for correction.
- **Server state machine:** new admin action `requestProviderChanges(id, reason)` (`requireAdmin`, mandatory trimmed reason, optimistic `updateMany WHERE status IN (APPLIED, UNDER_REVIEW)` → CHANGES_REQUESTED, audit `provider.changes_requested`, transaction). The reason **reuses the existing `rejectionReason` field** (no new column) and is cleared on the provider's next submit. **No notification** is dispatched — no CHANGES_REQUESTED kind exists and none was invented (provider sees it in-app).
- **Editable states unified via `isVerificationEditableStatus` (DRAFT ‖ CHANGES_REQUESTED):** the shared helper now gates the three document-mutation server guards (upload/replace/delete → APPLICATION_LOCKED otherwise), the submit guard, and the provider `editable` flag — single source of truth, no drift. Submit now allows re-submit from CHANGES_REQUESTED → UNDER_REVIEW (guarded `updateMany WHERE status IN (DRAFT, CHANGES_REQUESTED)`, sets `submittedAt`, clears `rejectionReason`). `assertProviderApprovable` (BR-029) unchanged — DRAFT and CHANGES_REQUESTED are never admin-approvable; approval still requires all required docs ADMIN-APPROVED.
- **Admin workspace enhancements:** `getAdminProviderVerificationChecklist` renders the FULL resolved requirement checklist (missing-required rows visible, not only in the blocker panel; orphan uploaded docs surfaced), a `requiredApproved/requiredTotal` progress summary, and `submittedAt` (exposed via `get-provider-detail`). Requirement-driven → COMPANY-ready with no IDENTITY_PROOF hardcoding. Reuses the Gate-3 View/Approve/Reject actions (no duplication; no objectKey/versionToken/signed-URL leakage — DTO still exposes only versionToken).
- **Provider CHANGES_REQUESTED UX:** status label/body + the admin reason on `/provider-application` and a banner on `/provider/verification`; documents editable again; explicit re-submit required (no auto re-submit). `CHANGES_REQUESTED` added to all four `Record<ProviderStatus>` maps; 8-locale strings for admin workspace + provider feedback.
**Staging:** the migration is NOT applied (local commit only; not pushed) — it reaches staging via `vercel-build` on a later, separately-approved deploy. **INDIVIDUAL policy unchanged** (Civil/National ID only; passport absent — the staging requirement-row was corrected manually earlier and is NOT re-touched).
**Alternative(s) considered:** Reusing `REJECTED → DRAFT` for "request changes" (rejected — overloads REJECTED with "denied" vs "please fix", muddying the applicant message + audit trail). A new dedicated reason column for changes (rejected — reusing `rejectionReason` is sufficient and avoids a second additive column). A separate `ProviderVerificationCase` model (still deferred).

### 2026-08-17 — Admin Backoffice Hardening · Gate A: backoffice-only admin identity + role-aware routing + review-first provider list (no schema)
**Context:** An ACTIVE Admin is a single BARQ `User` that may also historically hold `Customer`/`Provider` rows (shared-identity model). Nothing stopped an admin from exercising customer/provider PRODUCT capabilities (booking, paying, reviewing, applying/becoming a provider, uploading verification docs, editing customer settings). Product decision: **ACTIVE ADMIN = BACKOFFICE ONLY** — it may use only /admin, never customer/provider capabilities. Normal (non-admin) users keep full Customer + Provider coexistence, unchanged. Implemented as an authorization *exclusion* (Option 1) — **no schema change, no separate auth realm, no new Admin column**.
**Decision:**
- **One reusable authority, not scattered checks:** new `assertNotActiveAdmin(userId)` in `rbac.ts` throws `ForbiddenError("…backoffice-only", "ADMIN_BACKOFFICE_ONLY")`, reusing the existing `hasActiveAdminProfile()` (no duplicated admin-detection). It is baked into `requireCustomer()`/`requireProvider()` — which already gate ~40 correctly-layered customer/provider surfaces — and called explicitly at the **raw-prisma bypasses** that use `requireAuth()` + direct Customer/Provider access instead: `applyAsProvider` and `updateCustomerSettings` (mutations, mapped to a non-leaking `UNKNOWN_ERROR` so the hidden Admin row is never revealed), and the two API-reachable customer reads `getMyBookings` / `getBookingDetail` (the `/api/v1` auth wrapper maps the `ForbiddenError` to **403**). `requireApprovedProvider` inherits the exclusion via `requireProvider`. Notifications stay **role-agnostic** (userId-scoped) — an admin's own notifications are never blocked. `hasActiveAdminProfile` was made null/undefined-safe (`admin?.status === "ACTIVE"`) now that it runs on every customer/provider request.
- **Role-aware routing is SERVER-SIDE, never client hiding:** new non-throwing `isActiveAdminSession()` (pure lookup, no `resolveBarqUser` side-effect). Post-login `/login` and the universal `/dashboard` landing (also the Google `callbackURL`) send an active admin to localized `/admin`; the customer pages (`/dashboard`, `/dashboard/settings`, `/bookings`, `/bookings/[id]`, `/bookings/[id]/confirmation`, `/payments`, `/payments/[id]`, `/reviews`, `/services/[id]/book`, `/provider-application`) and the `/provider` layout each `redirect(...→/admin)` **before any customer/provider loader runs**. The domain guards remain the real boundary; the redirects are the clean-UX companion. `resolveCustomerNavOptions` suppresses the become-provider/workspace/application doorway for an active admin (the Admin Panel entry stays).
- **Review-first provider list (`/admin/providers`):** the inline one-click **Approve** button was REMOVED from list rows — a verification decision must not be made blind. Rows in a reviewable state (APPLIED / UNDER_REVIEW / CHANGES_REQUESTED) now surface a primary **"Review application / مراجعة الطلب"** link into the per-provider workspace `/admin/providers/[id]`, where `approveProvider()` still lives **unchanged** behind the full document checklist (BR-029). Each row summarizes the applicant at a glance (INDIVIDUAL/COMPANY type + submission date; `getProviders` extended with `providerType` + `submittedAt`). Non-verification lifecycle controls (Publish/Unpublish, Archive) stay on the row. New `reviewApplicationLabel` key added to all 8 locales.
- **Domain-layer authorization contract (final defense-in-depth amendment):** the backoffice-only invariant lives at the DOMAIN/authorization layer, NOT on the accident that a function is page-only today. The five Customer-capability reads that were originally protected by the page redirect ALONE now also call `assertNotActiveAdmin()` **inside the domain function** — `getDashboardData` (guards on its raw `barqUserId` arg, before any read), `getMyPayments`, `getMyPaymentDetail`, `getMyReviews`, `getCustomerSettings` — so a direct invocation from the BARQ API / iOS / Android / another server action is denied identically, regardless of route or UI. **Explicit split — ROUTING vs AUTHORIZATION:** an active admin *visiting* a Customer/Provider page is `redirect`-ed to `/admin` for UX; a *direct invocation* of any Customer/Provider domain capability is denied server-side (`ForbiddenError` / `UNKNOWN_ERROR` / API 403) irrespective of route or client. The page redirects are retained purely as UX routing and are never the security boundary. Full Customer + Provider domain sweep (every `requireAuth()` + raw `prisma.customer/booking/payment/review` / `prisma.provider`) confirmed no remaining Admin bypass; the only intentionally role-neutral exceptions are enumerated below.
- **Intentional role-neutral / non-Customer exceptions (deliberately NOT blocked):** notifications (`getNotifications`, `getUnreadCount`, `mark*NotificationRead` — userId-scoped, role-agnostic; an admin's own notifications); the dual-entitled resource routes `GET /api/bookings/[id]/history` and `GET /api/contracts/[id]/download` (owning-Customer ‖ active-owning-Provider ‖ **Admin** as a distinct entitlement branch, attributed `actorType: "ADMIN"` — admin precedence over the customer branch, so an admin there is exercising oversight, never acting as a customer); system/internal/public reads (`expire-stale-bookings` cron, `create-contract` + contract `notify` internal helpers, the payment-webhook processor, the public `get-service-detail` catalog); and auth/session + localization plumbing.
- **NOT in Gate A (explicitly deferred):** Admin notifications and any notification-schema change (future gate); Gate 1C (document intelligence); any change to booking/payment behavior beyond denying the active admin through existing Customer authorization; any Admin schema/realm change. No mobile-specific implementation added here (the domain guard simply makes future iOS/Android/API clients safe by construction).
**Verification:** NO schema migration (confirmed — `prisma validate` clean, no new migration authored). tsc + eslint clean; full vitest **2676/2676** green (all 359 files; `next build` succeeds; `git diff --check` clean). New/updated tests: `assertNotActiveAdmin` allow/deny + code, `isActiveAdminSession` (no-session / unlinked / non-admin / active-admin, and no `resolveBarqUser` side-effect), `requireCustomer`/`requireProvider` reject active admin before any row lookup while still admitting normal users, `applyAsProvider`/`updateCustomerSettings` → generic `UNKNOWN_ERROR` for an active admin (no Provider/Customer write), `getMyBookings`/`getBookingDetail` deny active admin before the Customer lookup, and (amendment) the five domain-layer reads — `getDashboardData`/`getMyPayments`/`getMyPaymentDetail`/`getMyReviews`/`getCustomerSettings` — each proven to deny an active admin before any Customer read via a direct (non-page) invocation while remaining a pass-through for normal users; plus `resolveCustomerNavOptions` doorway suppression and the `/dashboard` active-admin redirect (customer loaders never called). Pre-existing page tests updated for the new auth exports; the dashboard "admin sees Admin Panel nav" case was replaced by "active admin is redirected to /admin" (its old premise is precisely what Gate A changes). **Local commit only — NOT pushed, NOT deployed.**
**Alternative(s) considered:** A separate admin auth realm / dedicated admin `User` (rejected — heavier, diverges from the shared-identity model, and a plain exclusion is sufficient). A new `Admin.backofficeOnly` column or role flag (rejected — status `ACTIVE` already carries the meaning; no schema change is warranted). Client-only nav hiding (rejected outright — "no 'we hid the link' answers"; enforcement is server-side domain guards + server redirects). Blocking admin notifications too (rejected — notifications are role-agnostic and userId-scoped; an admin legitimately receives its own).

### 2026-08-17 — Admin verification review usability fix: review-first detail layout (no schema)
**Context:** Live human QA after Gate A found the admin had no obvious/effective path to inspect uploaded provider verification documents BEFORE approving. Audit: the admin detail page (`/admin/providers/[id]`) already contained the full apparatus — the resolved requirement checklist, each uploaded document's filename/size/upload-time/status, a labelled **View document / عرض المستند** action (→ `/api/provider/documents/[id]/view`, which authorizes an ACTIVE admin, mints a 60 s signed URL, never exposes the objectKey), per-document Approve/Reject with a mandatory server-validated reason (`REASON_REQUIRED`, trimmed, versionToken/STALE_DOCUMENT concurrency guard), provider-level Approve (server-gated by `assertProviderApprovable`), Reject and Request-Changes with mandatory reasons, and provider-facing display of every reason (document rejection + CHANGES_REQUESTED note on `/provider/verification`; provider rejection reason on `/provider-application`). Nothing was functionally missing or insecure — the defect was **layout order**: the Provider Actions (decision) card rendered ABOVE the Verification Documents (review) card, so the admin was steered to decide before scrolling to review.
**Decision:**
- **Review-first reorder (presentational only):** the detail page is now ordered summary → **Verification Documents (review)** → **Provider Actions (decision)** → related → audit. The admin inspects/View/approve-reject each document before reaching the provider-level decision controls. No functional element, route, server action, DTO, or authorization was changed (verified: every action/token count identical pre/post).
- **Approval-readiness affordance (Phase 10):** in the decision section, a pending application shows either the existing blocker panel + a disabled Approve (with `approveDisabledDocumentsHint`) while a required document is missing/unapproved, or — once `assertProviderApprovable` returns no blockers — an affirmative green "All required documents approved" banner (`verificationAllRequiredApproved`, reused key) immediately before the enabled Approve. The server gate in `approveProvider()` remains the sole enforcement; this is only its visible reflection so approval is never the first thing an admin sees. Introduced the derived `approvalReady = isPending && !hasBlockers` (rename of the prior inline condition) for clarity.
- **NO new i18n, NO schema, NO storage change:** all labels reuse existing admin keys; the document View mechanism (attachment-download signed URL) is unchanged (acceptable per the gate — no second storage mechanism invented). Document intelligence (Gate 1C) and Admin Notifications remain out of scope and untouched.
**Verification:** tsc + eslint + `prisma validate` + `next build` clean; full vitest **2682/2682** (359 files) — +6 new detail-page tests proving review-before-decision ordering, uploaded-document rendering with View + status + per-document controls, a MISSING required requirement still shown as a row, the admin seeing a document's rejection reason, and the blocked-vs-ready approval affordance. Two files changed (`admin/providers/[id]/page.tsx` + its test). **Local commit only — NOT pushed, NOT deployed.**
**Alternative(s) considered:** Changing the signed-URL disposition to inline-view instead of attachment-download (rejected — the gate explicitly accepts attachment if reliable and forbids inventing a second storage mechanism; inline rendering of user-uploaded content also carries its own handling concerns). Adding a distinct "Verification complete" i18n key (rejected — the existing `verificationAllRequiredApproved` already says exactly that; no 8-locale churn warranted). Splitting the review workspace into a separate route (rejected — it belongs on the one decision page, and a separate route would re-introduce the "review is elsewhere" friction this fix removes).

### 2026-08-17 — Gate B1: in-app notification events + ProviderCategory provenance (additive schema, staged migration)
**Context:** Gate B needs (A) an actionable in-app Notification Center and (B) a single-self-selected-activity policy with admin-granted additional activities. The audit found the `Notification` model had no `IN_APP` channel, no structured `eventType`, and no generic `entityType/entityId` (only `causingBookingId`; the "kind" was trapped inside the translated `content` JSON, and the UI had no per-notification CTA); and `ProviderCategory` was a bare M:N join with no way to distinguish a self-selected primary from an admin grant or a legacy link. Both required additive schema. This gate (B1) lands ONLY the schema + migrations; all runtime (notification writers/CTA/UI, one-activity onboarding, admin grant/revoke, service-category authorization) is deferred to B2–B5.
**Decision:**
- **Notification (additive):** `NotificationChannel + IN_APP`; `Notification.eventType String?` (stable machine key, open-ended like `AuditLog.action`, companion to the legacy `content.kind` which pre-B rows still render by), `entityType String?`, `entityId String? @db.Uuid`, `@@index([entityType, entityId])`. A raw URL is NEVER stored — the CTA route is resolved server/UI-side from (eventType, entityType, entityId) via an allowlist (B3).
- **ProviderCategory provenance (additive):** new enum `ProviderCategorySource { SELF, ADMIN, LEGACY }` and columns `source` (**NO `@default`** — provenance must be written explicitly by every runtime write; amendment 1), `isPrimary Boolean @default(false)`, `grantedByAdminId String? @db.Uuid` (FK → `admins`, `ON DELETE SET NULL`, never client-supplied), `grantedAt DateTime?`, `@@index([providerId, isPrimary])`. `Admin` gains the `grantedProviderCategories` back-relation.
- **Staged migration (2 files, amendment 1):** M1 adds every column additively with `source` **nullable** (Prisma-generated). M2 backfills deterministically — the oldest ProviderCategory per provider (createdAt asc, categoryId tiebreak) → `source=SELF, isPrimary=true`; **every other pre-existing row → `source=LEGACY`** (NOT ADMIN — no fabricated attribution), `isPrimary=false`, no admin fields — then a `DO`-block asserts no NULL `source` remains, `ALTER COLUMN source SET NOT NULL`, and finally the partial unique index. No row is deleted.
- **At-most-one-primary (amendment 2):** `CREATE UNIQUE INDEX … ON provider_categories(providerId) WHERE isPrimary` enforces AT MOST ONE primary per provider at the DB level (a partial index, not expressible in Prisma schema — intentionally unmanaged, as the model comment documents). "At least one primary" for a new provider is a DOMAIN invariant enforced by onboarding/DRAFT selection (B4), **not** by this index.
- **Write-compat glue only:** making `source` required (no default) forced the two existing self-selection writers (`apply-as-provider`, the deprecated `set-provider-categories`) to set `source: "SELF"` explicitly — the minimal change to keep the build green. This is NOT the B4 rework (no one-activity enforcement, no `isPrimary` designation, no admin grants); nothing reads `source`/`isPrimary` yet, so interim writes are inert.
**Verification:** `prisma validate` + `prisma generate` + `tsc` + eslint + `next build` clean; full vitest **2691/2691** (the sole full-suite miss is the known `next.config.test.ts` load-timeout flake — passes 4/4 in isolation). Migrations authored offline (`prisma migrate diff` schema→schema; reverse diff reviewed for rollback) — **NOT applied**; they reach staging only via `vercel-build`'s `prisma migrate deploy` on a separately-approved deploy. **Local commit only — NOT pushed, NOT deployed.**
**Rollback:** columns/indexes/FK/`ProviderCategorySource` are cleanly droppable; the `IN_APP` enum value is effectively irreversible once any row uses it (Postgres can't drop an in-use enum value) — forward-fix only, and B1 alone leaves it unused. **Alternative(s) considered:** keeping `@default(SELF)` on `source` (rejected per amendment 1 — provenance must never be assigned implicitly); Option A `Provider.primaryCategoryId` + separate grants relation (rejected in the audit — splits activities across two places, complicating the service-authorization union); claiming the partial index enforces "exactly one" (rejected per amendment 2 — it enforces at-most-one; "at least one" is a domain rule).

### 2026-08-17 — Gate B2: authoritative in-app notification write layer (no schema)
**Context:** Gate B1 (schema) is DEPLOYED on staging. B2 wires the actual IN_APP notification rows for provider-lifecycle events. No UI/CTA (B3), no activity-policy runtime (B4), no service-category enforcement (B5). No schema change (B1 delivered IN_APP + eventType/entityType/entityId).
**Decision:**
- **Central writers (no scattered `prisma.notification.create`):** `src/lib/notifications/create-in-app-notification.ts` — `createInAppNotification` (single recipient, channel ALWAYS IN_APP, stable eventType + server-derived entityType/entityId, no raw URL/objectKey stored) and `notifyActiveAdmins` (fan-out — one row per ACTIVE admin, recipients derived from `Admin.userId` server-side, zero-admins safe no-op). `src/lib/notifications/provider-notification-events.ts` — the stable eventType catalog (8 names), 8-locale content (no PII/reason/filename/objectKey/URL), and two **role-aware** dispatchers that THROW if an event's audience is mismatched (a provider event can never fan out to admins, and vice-versa).
- **Events wired post-commit, idempotent (fire ONLY on a real transition/success):** ADMIN fan-out — `provider.verification_submitted` (DRAFT→UNDER_REVIEW), `provider.changes_resubmitted` (CHANGES_REQUESTED→UNDER_REVIEW; `fromStatus` distinguishes the two), `provider.document_uploaded` (successful initial upload), `provider.document_replaced` (successful replace). PROVIDER — `provider.approved`, `provider.rejected`, `provider.changes_requested` (NEW — Gate 1B previously sent none), `provider.document_rejected`. A no-op/raced submit (`updateMany.count===0`), a stale/failed doc op, or a failed upload fires nothing.
- **Failure policy = post-commit best-effort** (the established booking/approve pattern): each dispatch is wrapped in its own try/catch, logged without PII, and can NEVER roll back or fail the durable mutation nor surface as the outer error. Notifications are UX; the AuditLog remains the authoritative history and is untouched.
- **Legacy compatibility:** new rows also carry `content.kind` for the existing presentation layer (legacy kinds keep their icons; new kinds fall back to the generic icon until B3 rebuilds rendering off eventType). Pre-B2 rows (channel EMAIL, `content.kind`, no eventType) are NOT backfilled and still render. The old `notify-provider-application.ts` (+ test) was deleted; its 3 events (approved/rejected/document_rejected) now flow through the central catalog with IN_APP + structured metadata (their channel changed EMAIL→IN_APP, which is the correct semantic — the in-app center reads by userId/readAt regardless of channel).
**Verification:** NO schema/migration; `prisma validate` + tsc + eslint + `next build` clean; full vitest **2698/2698** (sole miss is the known `next.config.test.ts` load-timeout flake, passes 4/4 isolated). New/updated tests cover: IN_APP write + eventType/entity persisted, admin fan-out (ACTIVE only, zero-admins safe), role-aware audience guards, content privacy (no objectKey/URL/reason) + 8-locale parity, and per-flow idempotency (verification_submitted vs changes_resubmitted, no dup on raced/no-op, no notif on failed upload / stale replace / stale review). **Local commit only — NOT pushed, NOT deployed.**
**Alternative(s) considered:** notifying inside the business transaction (rejected — a notification write must never roll back an approval/submission; the repo's established pattern is post-commit best-effort). Keeping `notify-provider-application.ts` and adding fields to it (rejected — Phase 2 wants a single central abstraction, not a second scattered writer). Storing the CTA href in the row (rejected — open-redirect risk; B3 resolves the route from eventType+entityType+entityId via an allowlist).

### 2026-08-17 — Gate B3: actionable Notification Center UI + safe CTA resolver (no schema)
**Context:** Gates B1 (schema) + B2 (IN_APP write layer) are deployed. B3 turns the existing bell/panel/pages into an actionable Notification Center. Presentation + read-state + CTA only — no writer change, no B4/B5, no schema (B1 delivered the columns).
**Decision:**
- **Safe CTA resolver (`src/lib/notifications/resolve-notification-action.ts`, pure, client+server):** a fixed per-eventType allowlist maps a notification's `(eventType, entityType, entityId)` to `{ labelKey, href }` or null. A raw href is NEVER read from the row or accepted from a caller; the only interpolated value (admin events' providerId) is validated as a strict UUID before entering `/admin/providers/<id>`. Routing is keyed on **eventType** (which carries a fixed audience) — an admin event resolves ONLY to `/admin/...`, a provider event ONLY to a provider route, so `entityType="Provider"` alone can never decide the destination. Unknown/legacy event or invalid entity → null (notification still renders as plain text). Even a mis-addressed CTA is harmless: each destination page enforces its own `requireAdmin()`/`requireProvider()` (Gate A intact).
- **CTA matrix:** admin `verification_submitted`/`changes_resubmitted` → Review application → `/admin/providers/[id]`; admin `document_uploaded`/`document_replaced` → Review document → `/admin/providers/[id]`. Provider `approved` → Browse your workspace → **`/provider`** (verified canonical workspace route); `changes_requested` → Review required changes → `/provider/verification`; `document_rejected` → Review document → `/provider/verification`; `rejected` → View application status → `/provider-application`.
- **DTO (no schema):** `getNotifications` now surfaces the existing B1 columns `eventType/entityType/entityId` (the api/v1 `toNotificationDTO` projection is unchanged — it still exposes only its minimal native shape). One shared `<NotificationActionLink>` (client, embeddable in the server pages) renders the resolved CTA through the localized `<Link>`; it renders nothing when the resolver returns null.
- **Surfaces:** the bell dropdown (responsive width on mobile), `/notifications`, and `/provider/notifications` all render the CTA per row. In the bell, activating a CTA marks the row read optimistically and closes the panel — navigation never waits on the mark-read (fire-and-forget, existing best-effort pattern); the pages keep their explicit per-row check + mark-all. Unread state stays weight/icon-based, not color-only. Read-state actions (mark-one binds id+userId, mark-all binds userId; both idempotent) are unchanged and now unit-tested.
- **Legacy compatibility:** resolution order is new `eventType` for the CTA, existing `content.kind` for icon/category presentation; presentation map extended with the B2 provider-verification kinds (legacy/unknown kinds still fall back to the generic bell). No legacy rows backfilled.
- **i18n:** 8 new `notifications` keys (5 CTA + 3 category) across all 8 locales, with the exact mandated Arabic CTA phrasing; locale parity holds (translation-completeness green).
**Verification:** NO schema/migration; `prisma validate` + tsc + eslint + `next build` clean; full vitest **2725/2725**. New tests: the CTA resolver (all admin/provider mappings, invalid-UUID/wrong-entityType/unknown-event → null, no absolute/external href, admin↔provider audience isolation), presentation map for the new kinds, `getNotifications` surfacing the 3 fields, and mark-one/mark-all ownership scoping + idempotency. **Local commit only — NOT pushed, NOT deployed.**
**Alternative(s) considered:** storing a resolved href on the row (rejected — open-redirect risk; the allowlist resolver is safer and keeps routing centralized). Deriving the route from the viewer's role instead of eventType (rejected — eventType already fixes the audience, and B2 guarantees recipient-role match; the destination page is the real authorization boundary regardless). A full mobile bottom-sheet rebuild (deferred — a responsive-width popover fits the existing design system without a larger rewrite).

### 2026-08-17 — Gate B4: provider activity governance — one self-primary + admin-granted (no schema)
**Context:** Gates B1 (provenance schema) / B2 (notif writer) / B3 (notif UI) are deployed. B4 enforces the product rule: a provider self-selects EXACTLY ONE primary activity; additional activities are ADMIN-only; LEGACY links are preserved. B1 already delivered `source`/`isPrimary`/`grantedByAdminId`/`grantedAt` + the `provider_categories_one_primary` partial index — **NO schema change** needed.
**Decision:**
- **Central policy** (`src/lib/provider/activities/activity-policy.ts`, pure): `MAX_PROVIDER_SELF_ACTIVITIES = 1`; `canProviderEditPrimaryActivity(status) = status === "DRAFT"` (the primary is locked after submission/approval — changing it would affect verification/services/public profile/future B5). Reader `get-provider-activities.ts` groups a provider's links by provenance (SELF primary / ADMIN / LEGACY), never filtering by source.
- **Provider self-service hardened (server-authoritative):** `applyAsProvider` now rejects 2+ categories (never truncates) and creates the single one as `source=SELF, isPrimary=true`. The dangerous multi-select `setProviderCategories` (which `deleteMany`'d ALL rows incl. ADMIN/LEGACY and recreated up to 20 SELF) was **deleted** and replaced by `setProviderPrimaryActivity` (single categoryId; DRAFT-only via `PRIMARY_LOCKED`; atomically replaces ONLY the SELF row — ADMIN/LEGACY untouched). A provider can never set source/isPrimary/grantedBy*, self-add a second activity, or delete an ADMIN/LEGACY link. Onboarding + settings UI switched from the multi-select checklist to a single-select radio picker; settings shows a read-only "My activities" (primary + admin-granted + legacy) with the DRAFT-only primary picker. The old checklist component + `get-my-provider-category-selection` were removed.
- **Admin domain (requireAdmin):** `grantProviderActivity` creates `source=ADMIN, isPrimary=false, grantedByAdminId=<session admin>, grantedAt=now` (provenance never from client), idempotent on an existing SELF/LEGACY/ADMIN link WITHOUT rewriting provenance (P2002 race → idempotent). `revokeProviderActivity` revokes ONLY an ADMIN link (SELF/LEGACY → `NOT_REVOCABLE`), and the **service-in-use check + delete run in one transaction** — a service in that category blocks revoke with `ACTIVITY_IN_USE` and NO service is ever deleted/re-categorised/unpublished. Both audit (`provider.activity_granted`/`_revoked`) and notify the provider post-commit. Admin UI added to `/admin/providers/[id]` ("Authorized activities" + grant dropdown of not-yet-linked categories + revoke on ADMIN rows only); no "make primary" control exists.
- **Notifications (no schema):** two new B2 events `provider.activity_granted` / `provider.activity_revoked` (PROVIDER audience, static privacy-safe content — no category id/name), extended the B3 CTA resolver (both → "Browse your workspace" → `/provider`) and the presentation map. Post-commit best-effort.
- **Native/API safety:** no `/api/v1` path mutates ProviderCategory — the only writers are the (now-hardened) web server actions, so a native client cannot self-grant. **B5 blocker reported (NOT fixed):** `create-service`/`update-service` validate category *assignability* but NOT provider-*authorization* — a provider can attach a service to any public category outside their SELF∪ADMIN∪LEGACY set. B4's only service-related enforcement is the revoke in-use block.
**Verification:** NO schema/migration; `prisma validate` + tsc + eslint + `next build` clean; full vitest **2752/2752** (sole full-suite miss is the known `next.config.test.ts` flake, passes 4/4 isolated). New tests: grant (ADMIN provenance + idempotency + auth), revoke (ADMIN-only + in-use block + service-not-deleted), set-primary (DRAFT-only + replaces SELF only + locked), policy, activities reader, apply (one-primary + reject-2+), plus notif event/CTA extension. Existing Gate A/1A/1B/B1/B2/B3 + booking/payment tests green. **No LEGACY row rewritten/deleted; no SELF silently converted to ADMIN; no service auto-mutated; no client-controlled provenance. Local commit only — NOT pushed, NOT deployed.**
**Alternative(s) considered:** a separate `Activity` model (rejected — Category is the existing activity primitive; no second table for naming). Requiring exactly-one at onboarding (kept at-most-one + the DRAFT picker to stay non-breaking; "require one at submit" flagged as a product decision, not invented here). Auto-reassigning/unpublishing services on revoke (rejected — separate business decision; revoke is blocked instead).
