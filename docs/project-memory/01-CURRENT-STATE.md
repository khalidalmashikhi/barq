# 01 — Current State (as-built inventory)

**Read this file first for any non-trivial task.** It captures what is actually real in the repository today, as distinct from what is planned (`03-PRODUCT-REQUIREMENTS.md`) or aspirational in the older architecture docs. Last inspected during the project-memory phase that created this file; re-verify against real files before relying on a claim here for anything security- or money-adjacent.

This file gives the capability-level summary; `15-DATA-DICTIONARY.md` gives the entity-level detail behind each row of the table below, `16-BUSINESS-RULES.md` gives the specific, ID'd rules (with honest enforcement status) that govern each capability, `18-DOMAIN-MODEL.md` gives the Bounded-Context grouping, and `21-ENGINE-SPECIFICATIONS.md` gives the per-engine Purpose/Responsibilities/Dependencies detail.

## Architecture

Single Next.js 15 (App Router) + React 19 + TypeScript application at the repo root — **not a monorepo** (no `turbo.json`/`pnpm-workspace.yaml`/`nx.json`). PostgreSQL via Prisma (62 models, `prisma/schema.prisma` — includes the 6 inert Slice C1 daily-offering foundations), Better Auth for phone+OTP authentication, next-intl for 8-locale i18n, deployed on Vercel (`vercel-build` script runs `prisma migrate deploy && next build`; one cron job, `expire-stale-bookings`, every 30 minutes).

```
src/app/          Next.js routes ([locale]/ pages + api/ route handlers)
src/lib/          domain logic: booking/, provider/, admin/, contracts/, otp/, auth/, audit/,
                  observability/, notifications/, services/, dashboard/, i18n/
src/components/   UI components, grouped by feature area
src/i18n/         next-intl config — locales.ts, routing.ts, request.ts
prisma/           schema.prisma, migrations/, seed.ts
messages/         8 locale dirs × 10 JSON namespaces (static, developer-maintained)
scripts/          env-schema.ts + validate-env.ts (predev/prebuild gate, fails fast on bad config)
docs/             12 numbered subdirectories + docs/08-governance/adr/ (10 ADRs)
```

## Per-capability reality check

| Area | State today |
|---|---|
| **Categories** | **Real (Phase 1.1/1.2) with a live relational taxonomy (corrected 2026-08-11 — the earlier "`Service.categoryId` does not exist" claim here was STALE and is contradicted by the actual schema/code, which is the source of truth).** `Category` is a self-referential tree (`SubCategory` collapsed in, ADR-0015) with admin-gated CRUD + admin UI (`/admin/categories`) via `src/lib/categories/`. **`Service.categoryId` now EXISTS** — a single nullable FK to the most-specific `Category` node (`onDelete: Restrict`), added by migrations `20260723112230_category_domain` + `20260805120000_taxonomy_v1_expand`, per **ADR-0014** (revised by **ADR-0015**, both Approved/Frozen/implemented). It is **writable from both the provider and admin UI** — `src/lib/provider/create-service.ts` + `update-service.ts` (provider "New/Edit Service" `CategoryField`) and `src/lib/admin/create-service.ts` + `update-service.ts` — always validated via `assertAssignableCategory`; category is **optional at create, required at publish** (`assert-service-publishable.ts`). `/services` does a **dual read** (`services/page.tsx`): a `?category=<slug>` that resolves to an effectively-PUBLIC `Category` filters by the real relational `categoryId`; only an unresolved slug falls back to the now-**`@deprecated`** `categoryKeyword` name-substring bridge (`getServices()`). The homepage grid (`categories-section.tsx`) is DB-driven via `getPublicRootCategories()` with a 6-card hardcoded fallback when no PUBLIC roots exist. Provider↔Category "areas of activity" is a separate many-to-many (`ProviderCategory`, written by `apply-as-provider.ts`/`set-provider-categories.ts`), distinct from `Service.categoryId`. Owned by Marketplace, Bounded Context #16 (`ADR-0013`). Open reconciliation: `Service.serviceType` (CTI discriminator, hardcoded `EXPERIENCE` at creation) vs `Category.serviceTypeKey` — deferred to P4 (**BR-028**). **Approved BARQ v1 customer-facing taxonomy (2026-08-11, `ADR-0016`):** roots `cars`(RENTAL)/`tours-experiences`(EXPERIENCE)/`marine-trips`(EXPERIENCE)/`transfers`(TRANSPORT) + `tours-experiences` children `tourist-guides`/`adventures`/`local-experiences`/`cultural-tours`. Materialized by a staging-only, idempotent, `APP_ENV=staging`-guarded bootstrap (`scripts/bootstrap-staging-taxonomy.ts` + tested core `src/lib/categories/staging-taxonomy-bootstrap.ts`), which also archives the two junk staging roots (slugs `1`/`2`) only when dependency-free. Staging currently (pre-bootstrap): 0 published services, 1 approved provider, the 2 junk roots. |
| **Category visibility states** (PUBLIC/HIDDEN/LINK_ONLY/INVITE_ONLY/SCHEDULED/ARCHIVED) | **Real (Phase 1.1).** `CategoryVisibilityStatus` enum, enforced by `category-visibility-policy.ts`'s transition matrix. See BR-004. |
| **Pricing** | `Price` model exists (amount, currency, status ACTIVE\|SUPERSEDED) — provider-managed only, no admin pricing-rule UI, no admin/provider/both-with-approval/RFQ control modes. |
| **Commissions** | `Commission` model exists but supports **only a fixed 3-tier percentage** (`TIER_12`/`TIER_10`/`TIER_8`). No fixed-fee, hybrid, zero, or manually-agreed commission concept. |
| **Provider verticals** (regulated activity authorization — Phase 3B Phase 1) | **LIVE ON STAGING at commit `6f83b2d` (2026-09-11): migrations 53 (`20260910120000_provider_verticals`) + 54 (`20260911120000_seed_vertical_verification_requirements`) are applied to the staging database and the code is deployed to `barq-staging`. Production (`barq`) remains UNTOUCHED, and the legacy vertical backfill (`backfill-staging-provider-verticals.ts`) has NOT been run.** A `ProviderVertical` capability (`TOURIST_GUIDE`/`RENTAL_COMPANY`), reviewed and approval-gated, kept SEPARATE from `Provider.providerType` and from `ProviderCategory` grants (which never confer regulated authority). `Service.offeringKind {TOUR, VEHICLE_RENTAL}` is derived server-side from the taxonomy (RENTAL type → VEHICLE_RENTAL; the `tourist-guides` category → TOUR; never all EXPERIENCE) so a regulated listing can't stay null-kind to bypass the gate. Publishing requires BOTH Provider=APPROVED and the vertical=APPROVED+**compliant**; compliance reuses the ADR-0017 requirement engine on new audiences (`VerificationRequirementAudience.TOURIST_GUIDE`/`RENTAL_COMPANY`) — an empty required policy FAILS CLOSED, and required documents whose requirement sets `evidenceExpires` must be APPROVED + unexpired (`ProviderDocument.expiresAt`, admin-confirmed at approval). Approval re-checks compliance inside the transaction. Suspension hides that vertical's published listings + freezes new bookings/publishing (confirmed bookings untouched; APPROVED never auto-churned). Minimum requirements are seeded by an idempotent staging bootstrap (`scripts/bootstrap-staging-vertical-requirements.ts`); a separate idempotent, cutover-bounded backfill (`scripts/backfill-staging-provider-verticals.ts`) classifies legacy listings + seeds PENDING_REVIEW candidate verticals (never auto-approves). `src/lib/provider/verticals/`, `src/lib/admin/verticals/`. |
| **Financial tracking** | Gross amount/currency, a confirmation-time snapshot of price+commission on `Booking`, payment capture/refund state (`PaymentStatus`), ledger movement (`WalletTransaction`). **Not tracked anywhere**: discounts, taxes, an explicit net-amount field, settlement/transfer-to-bank status. No bank-account field exists on `Provider` at all. |
| **Admin capabilities** | **Substantial, real CRUD across most of the marketplace** (corrected 2026-07-27 — a much earlier version of this row, claiming "exactly one admin capability," was stale and contradicted by the actual code; repository code is the source of truth). `src/app/[locale]/admin/` has real route groups for Providers, Services, Prices, Availability, Bookings, Categories, Feature Flags, Homepage Sections, **plus, added by the Admin Operations Platform phase, Customers (read-only, `/admin/customers`) and Reviews (read-only, `/admin/reviews`)**. `/admin` itself is now a real operational Overview dashboard (`getAdminOverview()`) — platform metrics (customers/providers/published services/booking-status breakdown/reviews+average rating/completed gross revenue by currency/database connectivity), operational queues (pending provider approvals, bookings awaiting provider, bookings in progress, recently cancelled, recent reviews), and Provider/Customer operational insight lists (recently joined providers, providers without services, providers with no completed bookings, providers with no recent booking activity in 30 days, recently registered customers, customers with most bookings, customers awaiting reviews) — replacing what was previously a bare redirect to `/admin/providers`. `src/lib/admin/` has ~70+ files (create/update/approve/archive/cancel/capture/refund/list/detail queries). RBAC: every admin query/action calls `requireAdmin()` itself in addition to the layout-level gate (defense in depth, not redundancy). Reviews admin remains **read-only by design** — the schema's `ReviewModerationState` (PUBLISHED\|FLAGGED\|REMOVED) is filterable but no code anywhere sets FLAGGED/REMOVED or exposes a moderation action; no review-lifecycle/moderation workflow exists. **Corrected 2026-07-27 (Payment Experience & Financial Operations phase)** — Payments now has a real, read-only admin UI (`/admin/payments`, `/admin/payments/[id]`, backed by the pre-existing `get-payments.ts`/`get-payment-detail.ts`, the latter additively extended with `providerId`/`serviceId`/`invoice` for cross-linking). `capture-payment.ts`/`refund-payment.ts` (Stripe-backed, defaulting to a no-op unless `PAYMENT_PROVIDER=STRIPE`) still exist in the backend but remain **intentionally unwired to any UI** — no capture/refund button, form, or action menu exists anywhere in the codebase. |
| **Provider onboarding** | 4-field apply form (`businessNameAr`/`businessNameEn` required, `businessDescriptionAr`/`businessDescriptionEn` optional) via `src/lib/provider/apply-as-provider.ts`. **Verification documents now exist as a code-complete but INERT system (Provider Verification & Documents, Gates 1–3):** a `ProviderDocument` model + a provider `/provider/verification` page + an admin review section, keyed to the `providerType` (INDIVIDUAL → identity evidence, COMPANY → commercial registration; optional tourism licence). **The approval gate is enforced server-side** — `approveProvider` refuses `INCOMPLETE_DOCUMENTS` until every required document is `APPROVED` (**BR-029**); existing APPROVED providers are grandfathered. **(Corrected 2026-08-14, Provider Gate PA audit — the prior "inert / pending / commits local-only" claim is now stale.)** The Gate 1–3 commits are pushed to `origin/staging`, and the `provider_documents` / `provider_verification_requirement` migrations are applied by `vercel-build`, so the tables exist on staging. `SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` / `SUPABASE_DOCS_BUCKET` are now set on Vercel (Production + Preview), so `isDocumentStorageConfigured()` returns true and the storage code path is **active, no longer fail-closed**. Still pending: a live end-to-end upload check (the physical Supabase bucket's existence was not confirmed from a provider session in this audit) and the default verification-requirements DB seed (the resolvers fail-closed to the same three defaults meanwhile, so approval behaviour is already correct). No other onboarding fields yet (municipal licence, tenancy agreement, bank info, website, Maps location, opening-hours are still absent). **BR-001 (approval gating) is enforced**: a provider whose `status` is not `APPROVED` cannot create, edit, publish, unpublish, archive, or duplicate a service, or create/edit/delete availability (`requireApprovedProvider()`, `src/lib/auth/rbac.ts`) — see `16-BUSINESS-RULES.md`. |
| **Provider resource models** | `Driver`, `Guide`, `Asset`/`Vehicle` exist but are resources a Provider *owns* (inventory), not alternate provider types or onboarding paths. **Phase 3C (Vehicle-based daily booking):** **Slice A (customer-shell polish — shared bottom nav + always-visible language switcher; presentation-only) is DEPLOYED to staging at commit `1f337654`.** **Slice B (Vehicle Capacity Foundation) is DEPLOYED to staging at commit `e3516f1` (migration 55 applied; production untouched):** the Prisma field `Vehicle.passengerCapacity` was renamed to `bookablePassengerCapacity` with `@map("passengerCapacity")` (physical column + all data preserved) and a NEW nullable `Vehicle.registeredSeats` column added — two DISTINCT concepts (bookable customer capacity vs official registered seats; `bookablePassengerCapacity <= registeredSeats` when known; provider-entered, never auto-derived, never a billing multiplier). A capacity edit on a trusted/under-review vehicle re-opens verification (APPROVED/SUBMITTED → DRAFT) via the existing lifecycle (never touches `Asset.status`). Migration `20260914120000_vehicle_registered_seats` is additive-only and **applied to staging (migration 55); production untouched; no backfill.** The **daily per-vehicle fare-calendar UI remains BLOCKED** until an authoritative `PER_DAY` per-vehicle pricing engine + a per-date availability/price contract exist (`PER_DAY` today is `DURATION_BASED_UNSUPPORTED`, fail-closed); it must never be faked from per-person `Price` rows. Vehicle-only rental vs guided-tour-with-vehicle stay legally/technically separate (Phase 3B verticals; Slice B touches no vertical authorization). See `17-CHANGELOG-DECISIONS.md` (2026-09-14). **Slice C1 (Vehicle-based daily-offering schema FOUNDATIONS) is DEPLOYED to staging (code commit `ef126e5`; deployed HEAD `d8d3d59` = a docs-only precision correction; migration 56 applied to staging only; production untouched):** six new INERT tables in two fully-separate vertical families — `RentalOffering→RentalOfferingDay→RentalStartTime` (rental) and `GuidedTourVehicleOffering→GuidedTourVehicleOfferingDay→GuidedTourVehicleStartTime` (guided tour) — plus 4 enums, for per-vehicle-per-selected-Oman-day pricing with **explicit provider-opened, fail-closed availability** (`OfferingDayState` defaults `BLOCKED`; a date is bookable only via an explicit `OPEN` day). `serviceDate` is `@db.Date` (UTC-midnight, no shift); currency at the offering; optional stricter `offeringCapacityOverride`; **passenger count is capacity-only, never a price multiplier**. NO `guideId` (guide assignment stays booking-level; guide-overlap is C3/E). One active offering per `(serviceId, vehicleId)` via a raw partial unique index (ARCHIVED exempt); all FKs `ON DELETE RESTRICT`. **Precision:** the separate tables preserve legal-domain separation and prevent accidental polymorphic reuse, but the DB does NOT enforce provider authorization — that a rental offering belongs to an approved/compliant `RENTAL_COMPANY`, a guided offering to an approved/compliant `TOURIST_GUIDE`, that `Service.offeringKind` matches the offering table, or that the Service and Vehicle share the same authorized provider. Provider ownership, vertical authorization, service-kind compatibility, vehicle ownership, compliance, and capacity validation are **fail-closed server-side responsibilities for C2b** (not yet implemented). Migration `20260915120000_vehicle_daily_offering_foundations` is additive/inert, previously disposable-DB validated and **now applied to STAGING ONLY (migration 56, via vercel-build; production untouched)**. **No backfill ran; migration 56 has no INSERT; no implemented app path writes the six tables** — so they are expected empty, but direct staging row-count verification was UNAVAILABLE (staging `DATABASE_URL` is a Vercel *Sensitive* variable), i.e. emptiness is expected from the controlled rollout but was NOT independently confirmed by a direct row-count query. **C2/C2b/C3/D/E remain unimplemented** — no daily calculator, no calendar read models/APIs, no provider CRUD, no booking/reservation, no fare-calendar UI. The future `VehicleReservation.bookingId @unique` relaxation is a **non-additive constraint change deferred to Slice E** (not C1). See `17-CHANGELOG-DECISIONS.md` (2026-09-15). **Slice C2a (shared per-vehicle-day pricing + Oman-calendar foundations) is DEPLOYED to staging at commit `dbd2f7a` (pure foundations only; NO new migration — C1's migration 56 remains the latest; production untouched):** PURE code only — internal `PER_VEHICLE_DAY` pricing-unit (valid via `isValidPricingUnit` but **non-bookable through legacy `Price`**: classified `DURATION_BASED_UNSUPPORTED`, excluded from `BOOKABLE_PRICING_UNIT_CODES`, rejected by `calculateBookingTotal`/create-booking/price-writes — all `PER_DAY` layers intact); an **isolated pure daily calculator** (`calculate-vehicle-daily-total.ts`, Decimal-safe, passengers never multiply, sums per-date rates); **canonical Oman calendar-date helpers** (extend `oman-time.ts`: `parseOmanDateKey`, `omanDateKeyFromDbDate` no-shift, `omanDayWindow`, `startMinutesToHHmm`, `omanPickupInstant`, `isOmanPastDateKey`); and a **shared normalized calendar contract** (types + `MAX_CALENDAR_WINDOW_DAYS=62`/`DEFAULT=32` + window validators). **No DB/auth/API/mutation/resolver/UI**; no schema/migration change. Guided daily offerings coexist transitionally with the legacy `TourServiceVehicle` pool (mutual-exclusion routing is a deferred C3 rule). Production untouched. See `17-CHANGELOG-DECISIONS.md` (2026-09-15). **Slice C2b-R (rental-offering authorization + provider write management) is DEPLOYED to staging at HEAD `740afd0` (component commits `1e7bf5c` initial + `da5121a` block-day correction + C2b-R2 docs + `740afd0` in-transaction provider/vertical authorization correction; `origin/staging` == local == deployed == `740afd0`); NO new migration — C1's migration 56 remains the latest (deploy reported "56 migrations found → No pending migrations to apply"; no migration 57; no backfill); no offering/day/start-time staging data created (server-only, no route/UI caller, not invoked against staging); post-deploy `/api/health` green with `paymentProvider: NONE` unchanged and no public rental/calendar API or offering UI (all 404); production (`barq`) untouched:** nine server-only domain functions under `src/lib/offerings/rental/` (create, update-commercial, publish, suspend, archive, bulk-open-days, block-day, set/clear-daily-override, manage-start-times) — the first slice to actually WRITE the rental offering tables. **NO API route, NO UI, NO customer calendar/booking, NO Booking/VehicleReservation write, NO guided-vertical code.** Provider identity is always session-derived (never client-supplied); a foreign Service/Vehicle/Offering → uniform `*_NOT_FOUND`. **Transaction authority (correction 2):** `requireApprovedProvider` runs pre-transaction only to establish the actor + provider id; every one of the nine mutations then re-reads the provider's mutable `Provider.status` on the tx client (`assertProviderStillApproved` → `PROVIDER_NOT_APPROVED`/`NO_PROVIDER_PROFILE`) and the draft-vertical eligibility on the tx client (via `assertCanCreateListing`, which gained an optional db param defaulting to global prisma so existing Phase 3B callers are unchanged) — so a provider suspended/rejected between the entry guard and the write cannot race a mutation past. READ COMMITTED (not a global snapshot); guarded `updateMany`s + DB constraints remain the concurrency authorities; no advisory locks / SERIALIZABLE added. Enforces: owned Service with `offeringKind = VEHICLE_RENTAL` + owned Vehicle; draft-eligible/APPROVED-compliant `RENTAL_COMPANY` vertical (full live re-check on PUBLISHED edits + publish, on the tx client); guarded lifecycle (DRAFT→PUBLISHED|ARCHIVED, PUBLISHED→SUSPENDED|ARCHIVED, SUSPENDED→PUBLISHED|ARCHIVED; ARCHIVED terminal; same-state idempotent; concurrent loser → `OFFERING_STATE_CONFLICT`); serviceId/vehicleId immutable (no re-parent); `Prisma.Decimal` 2dp positive money (never float/per-passenger/per-hour); offering-level currency changeable **only while DRAFT and only with no day overrides present**; capacity override = positive-int party-size ceiling ≤ verified (never `registeredSeats`, never inventory); bulk-open bounded to 62 inclusive Oman days, past rejected, atomic, BLOCKED kept unless `reopenBlocked=true`; `setDailyOverride`/`manageStartTimes` require an existing `OfferingDay` (`OFFERING_DAY_NOT_FOUND`, never auto-open); **`blockRentalDay` (per contract) CREATES a missing day directly as BLOCKED** (fail-closed — never customer-available, never OPENs a day), flips an existing OPEN→BLOCKED preserving its override + start-times, and is an idempotent no-op when already BLOCKED — the missing-day create is race-safe via `createMany({ skipDuplicates })` (`ON CONFLICT DO NOTHING`; concurrent losers re-read and converge, never overwriting an override), and audit distinguishes `day_created_blocked` from `day_blocked`; start-times a declarative desired-OPEN set (unique 0–1439 minutes; a bounded count ceiling of **≤ 48 configured entries/day**, NO spacing imposed; state-change not delete). **Publish decision (verified from code):** offering-publish does NOT require a PUBLISHED Service (`assertServicePublishable` needs an ACTIVE `Price` a daily rental lacks) — it requires vertical compliance + vehicle selectability + verified capacity + ≥ 1 OPEN non-past day. **This leaves a real blocker (NOT merely "deferred to the C2c read"):** a `VEHICLE_RENTAL` Service still cannot reach `Service.status = PUBLISHED`, and C2c cannot show a customer calendar while it is unpublished — closing that gap is **C2b-R2 (daily-rental Service publication bridge)** — now IMPLEMENTED as a LOCAL checkpoint (see below and `17-CHANGELOG-DECISIONS.md`, C2b-R2). Every re-read + write + `recordAuditEvent` runs in ONE transaction (audit failure rolls back); NO advisory reservation locks (C3). Allowlisted provider DTO only. Validated: `tsc`/ESLint clean, 71 unit tests + wider offerings/date suites green, production build + `prisma validate` pass, and a **disposable-Postgres rehearsal** (real 1→56 apply, dropped after) confirmed the partial-unique index (ARCHIVED-exempt), a parallel create-race (one wins / other `23505`), a **parallel block-missing-day race** (one insert, no `23505`, single BLOCKED row; `ON CONFLICT DO NOTHING` preserves an existing override), and all day/start-time uniques + CHECK families. **Slice C2b-R2 (daily-rental Service publication bridge) is DEPLOYED to staging at HEAD `f69aaeb` (component commits `3c15756` bridge + `c61e67d` transaction-authority correction + `f69aaeb` keyset-pagination/overflow correction; `origin/staging` == local == deployed == `f69aaeb`; NO new migration — C1's migration 56 remains the latest, deploy reported "56 migrations found → No pending migrations to apply"; no offering/day/start-time staging data; post-deploy `/api/health` green with `paymentProvider: NONE` unchanged and no C2c calendar/booking surface reachable — all 404; production `barq` untouched):** a `VEHICLE_RENTAL` Service may now reach `Service.status = PUBLISHED` through EITHER the unchanged eligible legacy `Price` path (Path A) OR at least one valid PUBLISHED daily `RentalOffering` (Path B) — a shared transaction-capable `evaluateRentalServicePublishable` re-reads **every fact on the transaction client** — Service kind/owner, provider APPROVED, and RENTAL_COMPANY vertical compliance **including the mutable vertical STATUS** (a global-Prisma leak in `evaluateVerticalCompliance` was fixed so status + policy + evidence all read through the supplied client), then per PUBLISHED provider-owned offering the candidate-local Vehicle selectability/verified capacity + valid money/currency/capacity + ≥ 1 OPEN non-past Oman day. Provider/service-global facts are evaluated ONCE (fail the whole check); candidate-local failures disqualify only that offering; candidates are scanned by DETERMINISTIC KEYSET pagination (order by unique `id`, `id > cursor`, page size 50, no start-times) with candidate-local readiness filtered in memory and ONE batched distinct OPEN-non-past-day query per page (no per-candidate N+1) — so publication succeeds if ANY valid candidate qualifies regardless of position. A high safety ceiling (`MAX_RENTAL_SERVICE_PUBLISH_CANDIDATES = 1000`) bounds the scan; reaching it while more candidates remain returns a distinct internal `CANDIDATE_LIMIT_EXCEEDED` (logged with serviceId + inspected count) that fails closed to the public `NO_ACTIVE_PRICE` rather than a false "no candidate qualified" — it never publishes. Wired into the single authoritative `assertServicePublishable` (+ provider/admin `transition-service-status`) with the daily path re-checked INSIDE the publish transaction (READ COMMITTED, NOT a global snapshot; rollback → `NO_ACTIVE_PRICE`; no locks/SERIALIZABLE). Legacy Path A, `BOOKABLE_PRICING_UNIT_CODES`, `calculateBookingTotal`, booking creation, and all non-rental Service kinds are UNCHANGED; no `PER_DAY`/`PER_VEHICLE_DAY` Price became bookable; `assertServicePublishable` itself was not weakened. Boundary-verified: a PUBLISHED priceless daily-rental Service is safe + non-actionable on every customer surface (headline null → "price unavailable"/omitted, `deriveBookability` UNAVAILABLE, disabled CTA, `/book` no-prices empty state) — that UI/API gap is intentionally left for C2c. Validated: `tsc`/ESLint clean, 22 new unit tests + full Service-publication + C2b-R + vertical/vehicle/document + C2a suites + full Vitest (556 files/4818) green, production build + `prisma validate` pass, disposable-Postgres rehearsal confirmed the nested `vehicle.asset` ownership filter + the OPEN-non-past `@db.Date` boundary. **Public C2c customer visibility remains UNIMPLEMENTED (no calendar API, no customer daily-rental booking, no fare-calendar). C2c, C2b-G, C2d, C3/E, and Slice D remain unimplemented.** **Slice C2c (public daily-rental calendar resolver + read API) is DEPLOYED to staging at HEAD `6e2c296` (component commits `be4f2a6` resolver+read-API + `6e2c296` keyset-pagination/bounds correction; `origin/staging` == local == deployed == `6e2c296`; NO new migration — C1's migration 56 remains latest, deploy reported "56 migrations found → No pending migrations to apply"; no staging offering data; post-deploy `/api/health` green with `paymentProvider: NONE` unchanged; the read API `GET /api/v1/services/{id}/rental-calendar` is LIVE + safe — bad window→400, unknown/non-rental→404 envelope — with NO calendar UI or customer rental-booking surface (all 404); production `barq` untouched):** a pure `resolveRentalServiceCalendar` + a READ-ONLY `GET /api/v1/services/{id}/rental-calendar?from&to` return, for a publicly-visible (PUBLISHED + provider APPROVED/visible) `VEHICLE_RENTAL` Service over a strict Oman window (`from<=to`, ≤62 days; default 32 from today), each eligible PUBLISHED offering's per-date CONFIGURED availability + authoritative daily price (`OPEN` → `dailyAmountOverride` when valid else base; malformed override fails closed as `NO_PRICE`; `BLOCKED`/`NONE`/past → unavailable), plus a common-currency lowest-rate aggregate (mixed currencies fail closed). Reuses the C2a calendar contract + Oman helpers and the C2b-R/C2b-R2 vertical/vehicle readiness authorities (no duplication); reads NO legacy `Price` for pricing; guided tables untouched; passengers never multiply; binary per-vehicle availability (no `remainingCapacity`). Fail-closed + non-enumerating: non-public/non-rental → 404, a non-compliant/empty case → an empty public calendar (cause never revealed); the response never leaks provider status/compliance/documents/`registrationNumber`/`registeredSeats`/audit/Prisma shapes. Candidate offerings are scanned by **deterministic keyset pagination** (order by unique `id`, `id > cursor`, page size 50, no offset) with candidate-local readiness filtered in memory and **one batched windowed day `findMany` per page** (no start-times, no N+1); two explicit bounds guard the scan — `MAX_RENTAL_CALENDAR_OFFERINGS = 100` (max eligible offerings returned; a 101st fails closed as `ELIGIBLE_OFFERING_LIMIT_EXCEEDED`, never silently omitted) and `MAX_RENTAL_CALENDAR_CANDIDATES = 1000` (inspected-row ceiling; reaching it with more rows → `CANDIDATE_LIMIT_EXCEEDED`), both mapped to a safe generic 500 with no fleet/provider leak and never a partial calendar (this replaced the initial silent `take:100`). `lowestAvailableDailyRate` is the complete Service-wide minimum (a calendar is returned only when the full eligible set fit the bound). **`availabilityBasis: "CONFIGURED"` — NOT reservation-confirmed: no daily-rental reservation-conflict reader exists yet, so C3/E must atomically revalidate vehicle-reservation conflicts before booking.** READ-ONLY (no booking/reservation/hold/offering/day/start-time write, no audit). Validated: `tsc`/ESLint clean, 37 new tests + full Vitest green, production build + `prisma validate` pass, disposable-Postgres rehearsal (windowed `@db.Date` gte/lte boundary-inclusive no-shift + candidate scope). **`availabilityBasis: "CONFIGURED"` was SUPERSEDED by Slice C3/E1 (below) → `"CONFIGURED_AND_RESERVATION_CHECKED"`.** **Slice C3/E1 (atomic daily-rental vehicle-reservation authority) is DEPLOYED to staging at HEAD `1be6775` (component commits `cb92c3a` foundation + `1be6775` idempotency correction; `origin/staging` == local == deployed == `1be6775`); migration 57 (`20260916120000_rental_vehicle_day_reservations`) APPLIED to staging ONLY (deploy log: "57 migrations found" → applied exactly once → Ready; production untouched); NO public acquire/hold/release/confirm API, NO Booking/payment integration, NO UI (all such probes 404); the C3/E2 customer-facing acquire API MUST require a non-empty idempotency key (nullable key is intentional for internal/no-key acquisition only). C2c remains DEPLOYED (now at `1be6775` after this rollout):** the authoritative server/DB foundation preventing the same physical vehicle from being HELD/CONFIRMED for the same Oman calendar day twice, supporting one day / a continuous range / non-consecutive days with atomic all-or-nothing multi-date acquisition, per-vehicle per-day pricing, and passenger-count-as-capacity-only. Legacy `VehicleReservation` reuse was REJECTED (booking-bound, UTC-interval, no status/expiry/price/customer/token — incompatible); a SEPARATE additive authority was built and the six legacy callers + guided tables are untouched. Additive migration `20260916120000_rental_vehicle_day_reservations` (**APPLIED to staging (migration 57, via vercel-build); production untouched; REWRITTEN in the idempotency correction below before any shared application**) adds enum `RentalReservationStatus (HELD/CONFIRMED/RELEASED/EXPIRED/CANCELLED)` + **TWO tables: a HEADER `rental_vehicle_day_hold_groups`** (the logical hold identity + quote snapshot: `holdToken`, customer/service/offering/vehicle FKs RESTRICT, `passengerCount`, `idempotencyKey?`/`requestFingerprint?`, `quoteFingerprint`, `totalAmount`/`currency`, additive-nullable `bookingId` for C3/E2; CHECKs total>0 + passengers>0) with **`UNIQUE(customerId, idempotencyKey)` as the DB-enforced GROUP idempotency arbiter** (NULL key exempt), **and a CHILD `rental_vehicle_day_reservations`** (one row per (vehicle, Oman `@db.Date` day): NOT-NULL `holdGroupId` FK → header + `vehicleId` FK, `serviceDate`, `status`, per-date snapshot `dailyAmount`/`currency`/`priceSource`, server-owned `expiresAt`, `releasedAt`; CHECKs dailyAmount>0/priceSource∈{BASE,OVERRIDE}/HELD-has-expiry/CONFIRMED-no-expiry) with the **raw PARTIAL UNIQUE INDEX `(vehicleId, serviceDate) WHERE status IN ('HELD','CONFIRMED')`** as the physical vehicle/day arbiter. TWO arbiters (READ COMMITTED + P2002 rollback + a bounded ≤3 retry for the rare group-unique rollback race); it touches NO existing table (Customer/Service/RentalOffering/Vehicle/Booking gained virtual back-relations only). `acquireDailyRentalHold` (server-only, `RENTAL_HOLD_TTL_MINUTES=10`, `MAX_RENTAL_HOLD_DATES=62`) validates + canonicalizes the selection, re-reads every authority IN the transaction (owner, public-gated owned offering, provider-global vertical compliance, candidate-local vehicle readiness, effective capacity — passengers validated but NEVER a price factor, each day explicit OPEN with override-else-base price via the shared C2a `calculateVehicleDailyTotal`), recomputes the quote and returns `PRICE_CHANGED` (no write) on drift, expires stale targeted HELD rows, then `createMany`s all N day rows all-or-nothing — a `(vehicle,day)` conflict rolls the whole tx back (no partial hold/price/audit) mapped to `VEHICLE_DATE_CONFLICT`; the success `AuditLog` is in-tx. Idempotency (DB-enforced by the header `UNIQUE(customerId, idempotencyKey)`, claimed FIRST in the tx): same key+fingerprint → **stable historical replay** of the same group (persists after release/expiry; never a fresh hold, never extends expiry); same key+different request → `IDEMPOTENCY_MISMATCH`; concurrent same-key requests (even on DISJOINT vehicles/dates) → exactly one group commits, the loser replays or mismatches. `getDailyRentalVehicleConflicts` (read-only, bounded, one query, CONFIRMED + unexpired-HELD only) is integrated into the C2c resolver ONCE per page → an OPEN day actively reserved becomes `VEHICLE_CONFLICT` (dayState stays OPEN), excluded from the lowest rate; **`availabilityBasis` is now `"CONFIGURED_AND_RESERVATION_CHECKED"`** (still READ-ONLY, `no-store`, non-actionable — final acquisition revalidates atomically). `releaseDailyRentalHold` (owner-only HELD→RELEASED, idempotent, non-enumerating) + `expireStaleDailyRentalHolds`/`expireStaleHoldsForVehicleDates` (bounded, only stale HELD→EXPIRED, never CONFIRMED; NO cron added). **Confirmation (HELD→CONFIRMED) is DEFERRED to C3/E2** (needs the real Booking-creation tx) — `CONFIRMED` + nullable `bookingId` exist but no confirm function (no misleading partial confirm). Validated: `prisma format`/`validate` clean; `tsc` 0; ESLint (touched) 0; **disposable-Postgres rehearsal 13/13** (partial-unique rejects a 2nd active row directly + under REAL two-client concurrency → 1 win/1 conflict/1 active; multi-date atomic rollback; CONFIRMED blocks; RELEASED/CANCELLED/EXPIRED don't; lapsed-HELD→EXPIRED→re-acquire; different-vehicle-same-day; non-consecutive; all CHECKs; `@db.Date` no-shift); **91 focused tests** (45 new reservation + 46 resolver incl. conflict-integration) + full Vitest; production build + `git diff --check` + secret/prohibited-scope scans clean. **NO Booking/payment/UI write; NO push/deploy; migration NOT applied to staging/production; no backfill; production (`barq`) untouched.** **Remaining: Slice D (fare-calendar UI / provider calendar management / multi-date selection / passenger form / cart / checkout / payment) — none started; C2b-G and C2d also remain unimplemented.** **Slice C3/E2 (customer rental hold APIs + atomic Booking confirmation) is DEPLOYED to staging at HEAD `cd6f28e` (implementation `1c882f4` + correction `cd6f28e`; `origin/staging` == local == deployed == `cd6f28e`); migration 58 (`20260917120000_booking_rental_snapshot`) APPLIED to staging ONLY (deploy log: "58 migrations found" → `Applying migration 20260917120000_booking_rental_snapshot` → applied exactly once → Ready; production untouched); post-deploy `/api/health` green (`database: ok`, `environment: ok`, `paymentProvider: NONE` unchanged); the three `/api/v1/me/rental-holds` endpoints are LIVE + fail-closed (unauthenticated acquire/release/confirm → 401 envelope; malformed id/body → safe 4xx, never 500); C2c remains reservation-aware; production (`barq`) untouched:** connects the C3/E1 hold authority to the canonical Booking domain. A rental Booking is a canonical `Booking` with a TOTALIZED `PER_VEHICLE_DAY` money snapshot (`bookingTotalSnapshot` = per-date SUM, `billableQuantitySnapshot`=1 so no unit×qty multiplication, `seats`=1) + a NEW additive nullable `bookings.rentalSnapshot Json?` (migration 58; the immutable customer-safe rental record — offering/vehicle/verified-capacity/passengerCount/sorted Oman dateKeys/per-date price/total/holdGroupId/quoteFingerprint — AND the rental discriminator; no `registrationNumber`/`registeredSeats`; passenger count is capacity-only, never a billing quantity). Customer APIs under `/api/v1/me/rental-holds` (POST acquire, DELETE `/{holdId}` release, POST `/{holdId}/confirm`): thin adapters over domain actions that re-derive the customer via `requireCustomer`, REQUIRE an idempotency key, rate-limit per customer, and return SAFE DTOs; 3 new inline `ApiErrorCode`s (`PRICE_CHANGED`/`HOLD_EXPIRED`/`HOLD_NOT_CONFIRMABLE`, no messages/*.json change). `confirmDailyRentalHoldAndCreateBooking` (one tx): re-read owner+hold+children (all HELD+unexpired), re-resolve the authoritative quote via the SHARED `resolveRentalDayQuote` (extracted so acquire+confirm are byte-identical), `PRICE_CHANGED` (fresh quote, no write, hold intact) on drift vs the customer-accepted quote, else create ONE Booking (PENDING_PROVIDER) + rental snapshot + link `group.bookingId` + GUARDED `updateMany(HELD, expiresAt>now → CONFIRMED, expiresAt null)` with a `count===N` assertion (throws→rollback) + claim the confirmation `BookingIdempotencyKey` LAST (DB arbiter) + in-tx audit — all-or-nothing. Confirmation idempotency = DB-backed (same key+request → replay the SAME booking incl. after cancellation; different → `IDEMPOTENCY_MISMATCH`; concurrent identical → one Booking). Expiry race closed by the guarded confirm (confirmation wins atomically OR expiry wins, never a Booking with expired children). Cancellation-sync primitive `cancelConfirmedDailyRentalReservations` wired IN-TX into `cancelBooking`/`admin cancelBooking`/`rejectBooking` (CONFIRMED children→CANCELLED; idempotent; non-rental no-op; `completeBooking`+`expireStaleBookings` unchanged — a completed rental retains its history, and rental bookings are slotless so stale-booking expiry never matches them). `acceptBooking` unchanged (a `VEHICLE_RENTAL` service resolves to `vehicleId:null` → no legacy `reserveVehicleForBooking`; TOTALIZED money resolves). NO lifecycle-hook dispatch (provider notification/email deferred). Validated: `tsc` 0, ESLint(touched) 0, prisma validate, disposable-Postgres rehearsal (migration 58 applies from 57-baseline + 10 DB proofs), focused confirm/actions/cancellation/routes/money + 195 rental + full booking-lifecycle suites (3 legacy termination suites green after in-tx wiring), full Vitest, production build. **Migration 58 applied to STAGING ONLY (57→58, exactly once, via the automatic vercel-build `prisma migrate deploy`); no backfill; no payment/UI; production (`barq`) untouched.** **Remaining before Slice D: rental-booking notification wiring, fare-calendar + provider calendar UIs, cart/checkout, payment/refunds, optional general stale-hold scheduler, localized rental-booking DTO surfaces. C2b-G and C2d also remain unimplemented.** **C3/E2 correction (local follow-up on top of `1c882f4`; migration 58 still disposable-DB-only; C3/E1 deployed `5620bc3`): (1) confirm now writes ONE authoritative quote everywhere — the guarded hold-group link also writes final total/currency/quoteFingerprint and each child is confirmed by an exact per-serviceDate updateMany that also writes final dailyAmount/currency/priceSource (Booking = snapshot = group = children = Σ children = accepted quote); (2) the confirmation BookingIdempotencyKey is now claimed EARLY (right after Booking create, before the hold link + child confirm) so a concurrent identical loser replays the winner instead of a misleading HOLD_NOT_CONFIRMABLE (guarded link bookingId-IS-NULL/count=1; bounded ≤3 retry on the rollback race) — proven with the REAL authority + real Postgres + two clients; (3) migration 58 gained an additive nullable Booking.providerResponseDeadlineAt (set at confirmation = now + RENTAL_PROVIDER_RESPONSE_HOURS, default 48h, env-tunable, never client-set) and expireStaleBookings now also expires rental PENDING_PROVIDER bookings past the deadline → EXPIRED + linked CONFIRMED children → CANCELLED atomically (slot expiry unchanged + still fires the hook; rental expiry skips the hook; accepted bookings never swept). **Fail-closed hardening (deployed `2165a02` then `d7e39d8`, 2026-09-18):** the rental-expiry arm identifies a rental by a real, non-null `rentalSnapshot` ANDed with the passed deadline — NOT by the deadline alone — via a TWO-STAGE fail-closed design: (stage 1) the query uses `{ not: Prisma.AnyNull }`, which excludes BOTH SQL NULL and the JSON `null` literal (`{ not: Prisma.DbNull }` alone still matched a JSON-null literal — proven against real PostgreSQL), and (stage 2) a runtime structural guard `isRentalBookingSnapshot` (stable `rentalOfferingId` discriminator; rejects `{}`/array/primitive/malformed that AnyNull cannot exclude) — a slotless booking reaching the sweep only via a malformed snapshot is skipped safely (no transition, no release, id-only log). Rental-vs-slot classification keys off the snapshot, so a stray/imported/repaired `providerResponseDeadlineAt` on a non-rental booking can never be swept by this branch (proven with a real-Postgres four-state null-semantics test); (4) a strict fail-closed parseRentalBookingSummary + a `rental` projection on customer/provider/admin booking read models + API DTOs, with the displayed party size = rental passengerCount (never the billing-neutral seats=1), no holdGroupId/quoteFingerprint/registrationNumber leak. Validated: tsc 0, ESLint(touched) 0, prisma validate, disposable-Postgres rehearsal (migration 58 from 57-baseline) + a RENTAL_DBPROOF-gated real-authority two-client DB proof (4/4 against disposable Postgres), full Vitest (572 files/4996 tests green, 1 file/4 tests skipped = the DB-proof gate), production build. DEPLOYED to staging at `cd6f28e`; migration 58 applied to staging only (57→58, once, via the automatic pipeline); no payment/UI; production (`barq`) untouched.** |
| **Translation** | 8 static locale JSON files (`messages/{ar,en,de,it,pl,fr,cs,ru}/*.json`), developer-curated per ADR-0010. No AI-assisted workflow, no draft/review/approved translation-state concept. |
| **Direct-contact exposure** | Already fully prevented — not via a policy engine, but because no contact-exposing feature has been built. "Contact Provider" is an honestly-disabled placeholder button (own code comment: "no backing messaging system"). No phone/WhatsApp/email is ever rendered to a customer. |
| **"ساعدني" / support channel** | **Does not exist.** Zero matches anywhere in the repo. |
| **Support tickets** | `SupportTicket` model + `SupportTicketStatus` enum exist in schema — **zero code references anywhere in `src/`.** 100% schema-only. |
| **Internal messaging** | **Does not exist.** No `Message`/`Conversation` model anywhere. |
| **Notifications** | Real `Notification` model + Notification Center (list/unread-count/mark-read) exist. Channel enum is `WHATSAPP \| EMAIL \| SMS` only — one-way, system-generated, no admin-configurable trigger rules. |
| **CMS / dynamic homepage** | **Corrected 2026-07-27** — a real `HomepageSection` model + admin CRUD (`/admin/homepage-sections`, ordering + visibility) **does** exist, and the public homepage (`src/app/[locale]/page.tsx`) **does** read it via `getHomepageSectionRenderOrder()`, gated by the `homepage_dynamic_sections` feature flag — this row previously claimed neither existed. Of the 13 registry section keys, only 3 (Featured Experiences, Providers, Stats) query real *content*; the rest render static JSX + i18n strings regardless of the flag. What's still genuinely absent: a way to edit a section's own content (copy/images) from the admin UI — only ordering/visibility are admin-controlled today. |
| **Feature flags** | **Real** (corrected 2026-07-27 — previously stale). A real `FeatureFlag` model (`key`, `enabled`, `description`) with admin CRUD (`/admin/feature-flags`, `src/lib/feature-flags/`) — global on/off only, no scoping/rollout logic per its own documented scope. |
| **AI Center** | `AIAgent` model + `AIAgentStatus` enum exist (governed by ADR-0008's 17 permanent AI boundaries) — **zero application code** reads/writes it. `LLM_GATEWAY_API_KEY` is a reserved, unused env var. |
| **Audit trail** | `AuditLog` model (added Phase 5.2) + `BookingStatusEvent`/`BookingContractEvent` exist and are written to on real mutations (provider approval, service publish/unpublish/archive, availability changes, booking status changes) — **but nothing reads them in any UI.** Write-only today. |
| **Permissions** | RBAC via `src/lib/auth/rbac.ts` — 4 fixed roles (`requireAuth`/`requireCustomer`/`requireProvider`/`requireStaff`/`requireAdmin`), solid and code-controlled, but role-based, not a granular/configurable permission engine. |
| **Workflow/Rule/Form-Builder engines** | **Do not exist as generic engines.** Two hardcoded, domain-specific state machines exist (`src/lib/booking/lifecycle/`, `src/lib/contracts/lifecycle/` + `src/lib/contracts/execution/`) — solid precedent for *how* to build a real engine later, but neither is generic or admin-configurable. No `Form`/`FormField` model exists. |
| **Business Engine** | **Does not exist.** Everything above is either hardcoded or narrowly provider/admin-specific — no unifying, admin-configurable business-rule layer. |

## Known drift between the architectural constitution and reality

- **ADR-0011 mandates versioned public APIs** (`/api/v1/...`). **(Corrected 2026-08-14):** the versioned surface now EXISTS — **public** (Gate 1: services/service-detail/availability/categories/providers), **authenticated customer** (Gate 2: `/me`, `/me/bookings*`, `/me/notifications*`; Gate 3: booking create/cancel + timeline + notification-read), and **authenticated provider READ** (Gate PB: `GET /api/v1/me/provider` state + `/profile` + `/services[/:id]` + `/availability` + `/bookings[/:id]` + `/verification`). All are thin JSON adapters over the existing `src/lib/**` domain readers (allow-list DTOs, shared locale/error/pagination helpers, money-as-string), reusing the same authoritative functions the web uses. **Provider notifications reuse the role-agnostic `/api/v1/me/notifications` feed (no separate provider feed exists — not duplicated).** **(Updated 2026-08-15, Gate PC:)** the **provider WRITE APIs (Gate PC)** now EXIST as thin `/api/v1` adapters — implemented **LOCALLY (commit `6520000` on `staging`), NOT yet pushed/deployed and VERIFIED BY TEST ONLY (not live-QA'd)**: `PATCH /me/provider/profile`; `POST /me/provider/services`, `PATCH /me/provider/services/{id}`, `POST /me/provider/services/{id}/publish`, `POST /me/provider/services/{id}/unpublish`; `POST /me/provider/availability`, `PATCH /me/provider/availability/{id}`, `DELETE /me/provider/availability/{id}`; `POST /me/provider/bookings/{id}/accept`, `.../reject`, `.../start`, `.../complete`. Each is a thin adapter over the existing Server Action (`createService`/`updateService`/`publishService`/`unpublishService`/`createAvailabilitySlot`/`updateAvailabilitySlot`/`deleteAvailabilitySlot`/`updateProviderProfile`/`acceptBooking`/`rejectBooking`/`startBooking`/`completeBooking`) — no new business logic. A pre-authenticating wrapper (`withApiV1ProviderMutation`) runs `requireProvider()` **before** the action so the action's browser `redirect("/")`-on-unauth path can never emit a 307 instead of a clean JSON 401; each action then applies its own stricter gate (`requireApprovedProvider`) internally. Ownership stays at the domain boundary (**no client `providerId`**; missing/not-owned → **uniform 404**, anti-enumeration); JSON bodies are adapted to the `FormData` the actions expect (absent = leave unchanged, `""` = clear); success re-reads via the same ownership-scoped GET reader and returns the existing DTO (**no raw Prisma objects**). Deferred within Gate PC (explicit): **standalone price mutation** (no provider price-edit domain exists — the initial ACTIVE price stays bundled in service creation; `update-service.ts` records price-edit as an un-made future decision), **verification-document upload (v1)**, **provider media mutations**, and **provider application/resubmission API**. **Provider payments/payouts: NOT STARTED.** Provider earnings-summary still deferred. The pre-`/api/v1` routes under `src/app/api/` (auth, provider media/documents, contracts, webhooks, cron) remain unversioned by design.
- **Native Android is an approved client as of 2026-08-16 (`ADR-0019`), with NO Platform code change.** Android joins iOS (`ADR-0018`) as a first-class `/api/v1` consumer: same backend, same `src/lib/**` domain truth, same Better Auth cookie session (no bearer/JWT), no direct DB/Supabase access, no duplicated business rules. Like `barq-ios`, the client lives in its **own repository (`barq-android`) — not in this one**; this repository owns the decision and the contract only. **Nothing in `src/`, `prisma/`, or the API surface was added, changed, or deprecated for Android** — the existing versioned surface already serves it. Gate 0A (governance + toolchain + a minimal native shell) is complete on the Android side; **no BARQ API, auth, or product integration has started**, so there is no Android-facing contract note yet (the iOS sibling, `23-IOS-API-AUTH-INTEGRATION.md`, has no Android counterpart until Android auth is actually implemented). Known accepted cost, recorded in `ADR-0019` §Consequences: with three clients and **no OpenAPI/generated schema anywhere in this repo**, `/api/v1` DTO and error-envelope shapes are hand-mirrored per client, so a server-side DTO change gives no compile-time signal to either mobile client.
- **ADR-0009 (Better Auth user-model separation) is still "Draft v0.1 — Architecture Review pending"** in its own document header, even though the schema already implements the decision it describes (`AuthUser` model exists, `Session`/`Account` already target it).
- **Oman phone identity is canonicalized server-side (P0-1 security hardening, 2026-08-15).** `normalizeOmanPhone` (`src/lib/otp/normalize-oman-phone.ts`) is wired into the Better Auth `hooks.before` for `/phone-number/send-otp` and `/phone-number/verify`, rewriting the phone to one `+968XXXXXXXX` E.164 form so the resend cooldown, daily send cap, `Verification.identifier`, Twilio delivery, verify lookup, and `User` identity all key on it; non-Oman/malformed numbers are rejected **before** any SMS is sent. Closes the SMS-pumping / rate-limit-bypass / identity-fragmentation risk. Better Auth + Twilio architecture unchanged, no schema change. **DEPLOYED to staging** (commit `6f95889`; existing users verified 3/3 canonical, no migration needed).
- **Durable distributed auth/OTP rate limiting (P1 security hardening, 2026-08-15).** A dedicated PostgreSQL `AuthRateLimit` table (`auth_rate_limits`) backs cross-instance fixed-window limiters wired into the Better Auth `hooks.before`: per-IP + per-canonical-phone caps on send-OTP and a per-IP cap on verify-OTP, **on top of** the preserved 30s cooldown + daily cap. Atomic single-statement UPSERT (no race), fail-closed on store error. **Trusted IP = Vercel's `x-real-ip` ONLY** (`x-forwarded-for` is NOT trusted — leftmost is spoofable; missing → shared limited `unknown` bucket). **Both identities are HMAC'd** with `BETTER_AUTH_SECRET` (IP hash + HMAC of the canonical +968 phone), so `auth_rate_limits` stores **opaque keys only — no raw IP or raw phone**, never logged raw. TTL via lazy reset + opportunistic sweep (no cron), `TOO_MANY_REQUESTS`/429 (no client change). **No Redis/Upstash/KV, no new secret.** Replaces reliance on the process-local in-memory limiter (which stays for booking/review only). **Migration `20260815120000_auth_rate_limit` created but NOT YET APPLIED to staging; VERIFIED BY TEST, live QA pending.** See `12-DECISIONS.md`.
- **Global-ready phone-entry UI, authentication still OMAN-ONLY (2026-08-15).** The web login's free-text phone field is replaced by a country selector + national field with a searchable Country Picker (`src/lib/countries/*`, `src/components/auth/{country-picker,phone-number-input,phone-entry}.*`). The **UI is globally structured but exactly one country is auth-enabled (Oman)** — others show "coming soon" and can never call send-otp. `resolveAuthPhone` reuses the P0-1 canonicalizer (never re-implemented), so the identity sent stays the exact `+968XXXXXXXX` contract; P0-1 + P1 remain the server authority. Search matches EN/AR (diacritic-tolerant) name, ISO, and calling code. No backend/schema/env/Twilio change. Enabling another country later is a paired UI+server change. **VERIFIED BY TEST; not pushed/deployed.** See `12-DECISIONS.md` and `23-IOS-API-AUTH-INTEGRATION.md` (iOS parity contract).
- `messages/ar/landing.json` uses the incorrect Arabic spelling **"بارق"** in 13 places; `messages/ar/common.json`'s `appName` correctly uses **"برق"**. See `13-OPEN-QUESTIONS.md`.

## Full engine-by-engine gap table

See `../plans/ROADMAP.md` for the complete current-state-vs-target breakdown of all 15 planned engines, and `AGENTS.md` for the summarized list.
